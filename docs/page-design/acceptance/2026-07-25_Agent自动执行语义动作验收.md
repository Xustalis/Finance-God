# Agent 自动执行语义动作验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_Agent自动执行语义动作设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `pages/交易台.md` 与 `02_前后端职责与数据合同.md` 已补充 `ui_actions` 链路 |
| 真实数据、时间与失败状态符合合同 | 不适用 | 未改变行情、账户或订单事实读取 |
| 主操作及权限边界正确 | 通过 | 服务端复用动作参数策略；提交、撤单、资金与设置不在目录 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 两种 decision 模式均可携带动作；Workflow 创建逻辑未改变 |
| 设置隔离与提醒留存 | 不适用 | 未改变设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | FE 全量 92 项、BE Agent 路由 12 项通过；ESLint 0 error；`vue-tsc --noEmit`、Vite build、Python compileall 通过；真实浏览器联调中 decision 与 3 个 `/desk/ui-actions` 均返回 200 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 不适用 | 无样式改动 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 无 DOM 改动 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 无样式改动 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 无布局改动 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 无布局改动 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 无行情展示改动 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 无行情展示改动 |
| 仿真账户/仓位/订单有「仿真」标注 | 通过 | Agent 只能预填既有仿真草稿，测试确认未调用提交接口 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 未改变快捷指令或工作流状态 UI |
| 「我的」/提醒/设置不进入 Agent 能力 | 通过 | 动作目录未扩展这些能力 |
| <1024px 同路由自适应，无独立移动流 | 不适用 | 无响应式改动 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 不适用 | 无 DOM 或动效改动 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| Agent 可自动选择标的、切换交易区并预填草稿 | 通过 | 真实浏览器输入“打开贵州茅台，切到交易页并帮我填买入100股”后，页面显示 `600519.SH`、买入、市价、数量 `100`；3 个动作回执均为 `applied` |
| 过期或拒绝动作不改变左侧 | 通过 | 既有 `applyUiAction` stale/rejected 测试继续通过 |
| Agent 不提交或撤销订单 | 通过 | 服务端目录拒绝测试 + FE 集成测试断言未调用提交；真实网络记录仅有 decision 与 UI action 请求，没有草稿创建、订单提交或撤单请求 |
