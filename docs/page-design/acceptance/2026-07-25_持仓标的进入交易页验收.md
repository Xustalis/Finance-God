# 持仓标的进入交易页验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_持仓标的进入交易页设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `pages/交易台.md` 已补充持仓标的导航合同 |
| 真实数据、时间与失败状态符合合同 | 通过 | 点击后复用 `setSymbol` 与交易区统一行情链路 |
| mock 仅用于获准的非交易只读模块，且模式、原因、时间、来源与恢复行为可验证 | 不适用 | 本次不涉及 mock |
| 主操作及权限边界正确 | 通过 | 标的点击仅导航；不预填、不提交订单 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 本次不修改 Agent |
| 设置隔离与提醒留存 | 不适用 | 本次不修改设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | 前端定向测试、类型检查与生产构建通过 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 标的入口沿用墨色文字和细下划线 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 通过 | 进入交易区复用现有工作区切换 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 无壳层变化 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 未修改行情合同 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 使用原生按钮，不改变布局 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 标的入口为原生 `button` 并带明确 aria-label |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 点击持仓标的进入对应交易工作区 | 通过 | 组件事件与 Pinia 导航均有定向测试 |
| 导航不预填或提交订单 | 通过 | 测试断言 `tradeDraftPrefill` 为空且未调用提交接口 |
| 卖出快捷入口行为保持独立 | 通过 | 既有卖出预填测试继续通过 |
