# 交易图表主题统一验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_交易图表主题统一设计简报.md` 与 `MarketChart.vue` 一致 |
| 受影响路由与页面规范已更新 | 不适用 | `/desk` 页面合同未改变，现有页面规范已要求纸墨令牌、规则线和真实行情 |
| 真实数据、时间与失败状态符合合同 | 通过 | 继续展示 PandaData、上游时间、频率、freshness；错误区域保持可见 |
| mock 仅用于获准的非交易只读模块，且模式、原因、时间、来源与恢复行为可验证 | 不适用 | 本次不改数据与 mock |
| 主操作及权限边界正确 | 通过 | 周期切换与分钟周期禁用条件保持不变 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 本次不改 Agent |
| 设置隔离与提醒留存 | 不适用 | 本次不改设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | 图表/布局测试 7/7；`vue-tsc --noEmit`；`vite build`；本地生产预览 HTTP 200 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 图表移除圆角卡片边框；蓝紫青均线改为棕墨谱系；普通 Agent 消息移除阴影与装饰折角 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 壳层未改 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 壳层未改 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 壳层未改 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | Agent 未改 |
| 涨跌仅用正/风险色，且有非颜色信号 | 通过 | K 线与成交量使用 `--positive`/`--risk` 对应色值；价格仍显示正负号 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 图表标题区和错误区保持原合同 |
| mock 未进入价格、估值、Agent/Evidence、风控、订单、成交或审计 | 通过 | 本次无 mock |
| 模拟账户/仓位/订单有「模拟」标注 | 不适用 | 本次不改模拟业务区 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 仅统一普通消息视觉；快捷指令数量、工作流状态和数据逻辑均未改变 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 本次不改能力目录 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 新增 680px 内图表头与周期栏折行；路由不变 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 周期按钮保留 `aria-pressed`，焦点使用风险色清晰描边；无新增动效 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 总览显示上游时间/频率/freshness，失败无假价 | 通过 | `MarketChart.vue` 保留完整数据元信息与错误通道 |
| 交易区复用总览 K 线且标的一致 | 通过 | 仅修改共享组件视觉，不改变 props、请求或状态归属 |
| 持仓、自选、复盘无记录时仍提供可操作信息 | 通过 | 三个工作区使用规则线空账页说明进入条件、正式字段与数据边界；未伪造业务记录 |
| “梳理基础信息”进入真实研究链路 | 失败 | 已实现并补测试用例，但本机 Python 环境缺少 `pytest_asyncio` 与 `sqlalchemy`，后端测试未能执行 |
