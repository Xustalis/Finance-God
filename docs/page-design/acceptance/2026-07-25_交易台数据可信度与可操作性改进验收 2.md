# 交易台数据可信度与可操作性改进验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_交易台数据可信度与可操作性改进设计简报.md`；实现覆盖其中的事实隔离、账户门禁、引用行情、提醒语义和窄屏表格。 |
| 受影响路由与页面规范已更新 | 通过 | 仅影响 `/desk`；`pages/交易台.md` 已补充最后成功时间、账户四态、引用行情和提醒隐藏语义。 |
| 真实数据、时间与失败状态符合合同 | 通过 | `tradingDesk.ts` 按请求标的校验事实批次并记录 `marketLoadedAt`；`OverviewWorkspace.vue` 同时展示同标的保留数据和刷新错误，不渲染其他标的数据；负向组件测试通过。 |
| 主操作及权限边界正确 | 通过 | 账户状态为 `absent` 才开放建账；读取失败不渲染建账表单；草稿创建要求账户 `available` 且真实引用行情可用。 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 本次未改变 Agent 能力；浏览器快照仍为三条快捷指令、服务端工作流入口和当前工作区/标的上下文。 |
| 设置隔离与提醒留存 | 通过 | Toast「隐藏」只设置本地可见性；单元测试验证未调用 `markNotificationRead`，未读计数保留。 |
| 单元测试、类型检查、构建和烟测 | 通过 | 全量 Vitest 74/74；最终定向测试 18/18；`vue-tsc --noEmit` 通过；Vite 生产构建通过；Playwright 在 1440×1000 与 680×900 完成真实页面快照和交互烟测。 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 新增引用行情使用既有纸面、墨色、规则线和风险色；未增加卡片或新强调色。 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 通过 | Playwright 1440px 快照显示 `FINANCE GOD`、副标、提醒、我的。 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 通过 | 既有 `.desk-nav` 结构保持；浏览器快照显示四个横向工作区按钮。 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 通过 | 1440px 烟测显示左工作区、可聚焦 separator 和展开 Agent。 |
| Agent 宽度可调（约 32%–60%）或有布局菜单等价入口 | 通过 | 既有 `role="separator"`、布局菜单和 32–60 范围测试继续通过。 |
| Agent 收起后可见 rail（非模态/气泡） | 通过 | 既有 rail 持久化测试通过。 |
| 涨跌仅用正/风险色，且有非颜色信号 | 通过 | 行情仍显示 `+/-` 数字符号；本次未改变语义色。 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 1440px 快照显示上游时间、`1m · current` 和最后成功读取时间；错误只保留真实旧数据。 |
| 仿真账户/仓位/订单有「仿真」标注 | 通过 | 持仓与交易章节继续明确标注仿真；本次新增账户状态文案同样使用「仿真账户」。 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | Playwright 总览、持仓、交易快照均为三条快捷指令。 |
| 「我的」/提醒/设置不进入 Agent 能力 | 通过 | 未修改 Agent capability；提醒改动仅限 Toast 本地可见性。 |
| <1024px 同路由自适应，无独立移动流 | 通过 | Playwright 680×900 仍为 `/desk`，左工作区与 Agent 上下排列。 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 行情标的选择由点击 `<tr>` 改为有 `aria-pressed` 的原生按钮；表格、separator 和既有 reduced-motion 规则保留。 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 顶栏仅字标 + 提醒 + 我的，无蓝链导航 | 通过 | 1440px 浏览器快照。 |
| 四标签横向，当前朱红下划线 | 通过 | 1440px 浏览器快照与 `.desk-nav button.active::after`。 |
| 宽屏可拖分栏；收起后 rail 仍可见 | 通过 | separator 键盘测试与 rail 重挂载测试通过。 |
| 总览表格有上游时间/频率/freshness；失败无假价 | 通过 | 浏览器快照与 `OverviewWorkspace` 负向测试。 |
| 持仓/钱包/订单标注仿真 | 通过 | 持仓、交易及「我的」现有测试通过。 |
| Agent 三条快捷指令 + 真实工作流状态 | 通过 | 浏览器快照与既有工作流渲染。 |
| 「我的」与提醒不进入 Agent 能力 | 通过 | 未改变 Agent 协议；提醒隐藏测试未调用服务端已读动作。 |
