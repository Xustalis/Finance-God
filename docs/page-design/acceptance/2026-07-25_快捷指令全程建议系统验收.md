# 快捷指令全程建议系统验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `docs/page-design/briefs/2026-07-25_快捷指令全程建议系统设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `pages/交易台.md` 与 `02_前后端职责与数据合同.md` 已记录三阶段和新接口 |
| 真实数据、时间与失败状态符合合同 | 通过 | 回答按 owner/decision 读取；工作流只读 completed Run 的 final Evidence；失败返回空数组与可见错误 |
| 主操作及权限边界正确 | 通过 | 快捷指令仍走统一 Agent 提交；活动 Run 禁用；质量门拒绝订单、撤单、资金和设置动作 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | initial/after_answer/after_workflow 测试覆盖；context_version 阻止跨上下文响应 |
| 设置隔离与提醒留存 | 通过 | 新合同不接收设置、凭据或原始画像；提醒实现未改动 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端相关回归 67 项；前端 97 项；type-check、lint、build；本地浏览器验收 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 工具条仅使用现有 `--ink`、`--muted-ink`、`--rule` |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 通过 | 顶栏未改动 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 通过 | 左侧工作区未改动 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 通过 | 浏览器 1440px 为 720/720 双栏，1024px 为 512/512 双栏 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 通过 | Agent 壳层未改动 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 本次不改行情语义 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 本次不改行情数据合同 |
| 仿真账户/仓位/订单有「仿真」标注 | 不适用 | 本次不改仿真交易展示 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 浏览器首屏为 3 条；前端测试覆盖回答与工作流终态替换 |
| 「我的」/提醒/设置不进入 Agent 能力 | 通过 | 生成请求不含相关数据或能力 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 浏览器 900px 为单列且无页面级横向溢出 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 原生 button、disabled 和现有 focus/reduced-motion 规则；无新增动画 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 三阶段建议承接当前正式结果 | 通过 | 生成器提示与 API 测试验证 answer/Evidence 由服务端注入 |
| 近义重复、空泛指令和越权动作被拒绝 | 通过 | `test_multi_agent.py` 覆盖近义/完全重复、修复重试和禁止动作 |
| 迟到响应不污染新标的 | 通过 | `rebuilding.spec.ts` 覆盖切换标的后忽略迟到结果 |
| “换一组”不绕过 Agent 提交流程 | 通过 | 浏览器可见且可触发；前端测试确认只调用建议接口 |
