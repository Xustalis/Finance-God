# Agent 工作流开始执行消息验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `docs/page-design/briefs/2026-07-25_Agent工作流开始执行消息设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `docs/page-design/pages/交易台.md` 已补充状态摘要要求 |
| 真实数据、时间与失败状态符合合同 | 通过 | 标题仅映射服务端 `WorkflowRun.status`，未增加本地计时状态 |
| 主操作及权限边界正确 | 不适用 | 未改变操作或权限 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 正式任务仍使用统一 Agent 时间线及服务端工作流状态 |
| 设置隔离与提醒留存 | 不适用 | 未改动设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | `desk-agent-messages.spec.ts` 10 项通过；`vue-tsc --noEmit` 通过；ESLint 0 错误；Vite 构建通过 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 未改动样式与令牌 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 未改动顶栏 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 未改动工作区导航 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 通过 | 仅修改现有 Agent 消息摘要文案 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 通过 | 未改变 Agent 布局 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 未展示行情涨跌 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 未改动行情 |
| 模拟账户/仓位/订单有「模拟」标注 | 不适用 | 未改动模拟业务事实 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 摘要文案直接来自真实状态映射，快捷指令未改动 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 未改动能力目录 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 沿用同一消息 DOM，无宽度特定变更 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 保留原有 `details/summary` 语义和交互 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| Agent 时间线显示服务端执行方式和真实任务状态 | 通过 | 工作流作为对话消息呈现；活动状态默认展开，`queued` 显示“正式任务已受理”，`running` 显示“正式任务已开始执行” |
| 活动 Run 不被覆盖且可持续更新 | 通过 | 状态仍由 store 原位替换同一 `workflow` 消息 |
| 活动工作流直接可见且终态可回看 | 通过 | 活动态默认展开 `.workflow-message-detail`；终态保留折叠回看 |
