# 交易台 Bootstrap 状态一致性验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_交易台Bootstrap状态一致性设计简报.md` 描述加载、成功、失败和重试状态 |
| 受影响路由与页面规范已更新 | 通过 | `pages/交易台.md` 联动规则新增 bootstrap 状态一致性要求 |
| 真实数据、时间与失败状态符合合同 | 通过 | 仅采用 `/desk/bootstrap` 返回的 `context_version`；失败保留真实错误，过期请求不回写 |
| 主操作及权限边界正确 | 通过 | 只有 `bootstrapStatus=ready`、版本存在且服务端能力满足时才启用 Agent 提交 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 上下文变化立即清除旧版本；成功 bootstrap 同步快捷指令、动作目录与能力 |
| 设置隔离与提醒留存 | 不适用 | 本次未改动设置与提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | Vitest 7 个文件、95 项通过；`npm run type-check` 通过；ESLint 0 error；`npm run build` 通过 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 复用 `.workflow-note`、`.data-error`、`.refresh-button`，无新令牌或卡片 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 顶栏未改动 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 工作区导航未改动 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 壳层布局未改动 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 通过 | 状态提示位于现有 Agent 线程内 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 未改动行情展示 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 未改动行情数据 |
| 仿真账户/仓位/订单有「仿真」标注 | 不适用 | 未改动仿真数据 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 未改变服务端快捷指令与工作流事实来源；仅增加 bootstrap 状态门禁 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 能力目录未扩展 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 复用现有 Agent 线程布局，无新路由或断点 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 重试使用原生按钮；加载使用 `role=status`，失败使用 `role=alert`；无新增动效 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| Bootstrap 加载、成功、失败状态可区分 | 通过 | `desk-agent-messages.spec.ts` 验证加载文案、失败原因和重试入口 |
| 上下文变化后旧版本立即失效 | 通过 | `rebuilding.spec.ts` 验证刷新开始时版本为空 |
| 迟到请求不得覆盖最新上下文 | 通过 | `rebuilding.spec.ts` 验证旧请求失败不会覆盖后续成功结果 |
| 活动 Run 禁止覆盖并可在刷新后恢复 | 通过 | 原有实现未改动；全量前端测试通过 |
