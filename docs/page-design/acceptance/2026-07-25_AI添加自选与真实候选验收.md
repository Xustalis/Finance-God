# AI 添加自选与真实候选验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_AI添加自选与真实候选设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `pages/交易台.md` 已增加 AI 加入自选与真实候选条件 |
| 真实数据、时间与失败状态符合合同 | 通过 | 候选服务只保留 provider 为 PandaData 且含上游时间的快照；全量失败返回 `MARKET_DATA_UNAVAILABLE` |
| mock 仅用于获准的非交易只读模块，且模式、原因、时间、来源与恢复行为可验证 | 不适用 | 本次不新增或使用 mock |
| 主操作及权限边界正确 | 通过 | `add_to_watchlist` 只接受规范 A 股代码，只加入当前分组 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 动作只在明确“加入自选”意图下提议，并复用版本化 `context_version` |
| 设置隔离与提醒留存 | 不适用 | 本次未改设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端目标测试 47 项、前端目标测试 46 项、`vue-tsc` 与 Vite build 通过 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 不适用 | 无视觉改动 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 未改顶栏 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 未改标签 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 未改壳层 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 未改壳层 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 未改涨跌 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 缺少真实行情的标的不进入候选表 |
| mock 未进入价格、估值、Agent/Evidence、风控、订单、成交或审计 | 通过 | 候选只接受真实 PandaData 快照 |
| 模拟账户/仓位/订单有「模拟」标注 | 不适用 | 未改模拟业务展示 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 未改快捷指令或工作流展示 |
| 「我的」/提醒/设置不进入 Agent 能力 | 通过 | 新动作只涉及自选标的写入 |
| <1024px 同路由自适应，无独立移动流 | 不适用 | 无布局变化 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 不适用 | 无交互控件变化 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| AI 明确指令可加入当前自选分组 | 通过 | Agent 路由、服务端动作回执和 Pinia 写入链路均有定向测试 |
| 非法标的或无自选分组明确失败 | 通过 | 服务端正则拒绝非法代码；前端无分组时抛出可见错误 |
| AI 不可创建/删除分组、移除标的或下单 | 通过 | 动作目录仅新增 `add_to_watchlist` |
| 候选只展示真实 PandaData 行情 | 通过 | 部分失败测试仅保留成功标的；空批次返回零候选和不可用原因 |
