# 交易产品前端清空重构验收

## 1. 任务与信息架构

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 页面首屏能识别用户任务、当前对象和主要状态 | 通过 | `/rebuilding` 显示“交易工作区正在重构”及不可用范围。 |
| 每个区域只有一个明确职责 | 通过 | 重构页只说明状态并提供返回画像报告入口。 |
| 主操作唯一且结果可预测 | 通过 | 唯一操作为“返回画像报告”。 |
| 页面与上一步、下一步对象保持连续 | 通过 | 画像方向保存成功后跳转至 `/rebuilding`，方向保留在既有后端服务。 |
| 没有无功能价值的区域或卡片 | 通过 | 已删除交易工作区、AI、行情和交易组件。 |

## 2. 文案

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 标题直接说明区域内容或操作 | 通过 | 标题为“交易工作区正在重构”。 |
| 辅助文字说明范围、操作、时效或决策价值 | 通过 | 说明暂不可用的交易区域与保留的画像方向。 |
| 删除了口号、隐喻、设计说明和重复文案 | 通过 | 页面仅含状态说明与返回操作。 |
| 按钮使用“动词 + 对象/结果” | 通过 | 入口文字为“返回画像报告”。 |
| 错误、空状态和恢复条件表达明确 | 不适用 | 本页不加载异步交易数据；画像保存失败沿用既有明确错误状态。 |

## 3. 真实数据

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 市场数据来自 PandaData，不含硬编码行情 | 不适用 | 重构期间浏览器不加载市场数据。 |
| 显示来源、供应商时间、实际频率和新鲜度 | 不适用 | 重构页不展示市场数据。 |
| 页面刷新间隔与数据频率分别表达 | 不适用 | 重构页没有市场数据轮询。 |
| 请求失败不会回退到模拟行情 | 不适用 | 已删除行情请求与展示。 |
| 真实行情与仿真账户/订单明确分离 | 不适用 | 已删除行情、账户和订单界面。 |
| 浏览器不可访问 PandaData 凭据 | 通过 | 已删除交易 API 客户端、行情 store 及其全部源码引用。 |

## 4. 工作区与桌面适配

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 1440 px 下信息层级和密度符合规范 | 不适用 | 当前没有交易工作区；仅保留响应式单栏重构说明。 |
| 1024 px 下主要任务仍可完成且无页面横向溢出 | 不适用 | 当前没有交易任务。 |
| 小于 1024 px 保持当前路由、当前对象与主任务，并将可收起面板默认收为可见轨道 | 不适用 | 已删除交易侧栏与工作区面板。 |
| 没有移动导航或移动页面 | 通过 | 未新增移动交易流程。 |
| 相关面板的显示、隐藏和尺寸行为符合页面规格 | 不适用 | 已删除交易面板。 |
| 工作区布局可以保存、恢复和重置 | 不适用 | 已删除交易布局偏好。 |

## 5. 视觉

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 使用暖米色报纸终端、衬线字体和紧凑专业工作区视觉 | 不适用 | 当前页面不是交易工作区；沿用全局暖纸色与字体令牌。 |
| 页面没有卡片墙、装饰渐变或常规阴影 | 通过 | 重构页仅为单栏文本与链接。 |
| 常规强调、状态、边框和装饰未使用蓝色 | 通过 | 使用现有墨色、纸色和风险色令牌。 |
| 红绿只用于交易、损益和状态语义 | 不适用 | 重构页不表达交易、损益或实时状态。 |
| 数字精度、单位、对齐和时区一致 | 不适用 | 重构页不展示数值。 |

## 6. 状态与交互

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 加载、正常、空、延迟、过期和失败状态已覆盖 | 不适用 | 已删除交易数据加载与轮询。 |
| 行情刷新不会清除选择或未提交输入 | 不适用 | 已删除行情刷新。 |
| 隐藏标签页停止轮询，恢复后立即刷新 | 不适用 | 已删除轮询控制器。 |
| 相同请求由统一控制器去重 | 不适用 | 已删除交易数据请求。 |
| 仿真订单状态未知时不能重复提交 | 不适用 | 已删除订单界面与提交逻辑。 |

## 7. AI

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| AI 分析绑定当前对象和数据版本 | 不适用 | 已删除前端 AI 侧栏与调用。 |
| 显示支持证据、反方证据和未知项 | 不适用 | 已删除前端 AI 与证据界面。 |
| Agent 只修改允许的 UI 状态和未提交草稿，不提交/撤销订单 | 不适用 | 已删除 Agent 与订单 UI。 |
| 统一交易台常驻同一个 Agent 侧栏 | 不适用 | 交易台已暂停重构。 |
| 侧栏可收起为可见窄栏并恢复展开 | 不适用 | 已删除侧栏。 |
| 展开/收起偏好可在刷新后恢复，恢复默认符合页面规格 | 不适用 | 已删除侧栏偏好。 |
| 窄窗口下侧栏默认状态符合页面规格 | 不适用 | 已删除侧栏。 |
| AI 侧栏不使用遮罩且不会遮挡主要操作 | 不适用 | 已删除侧栏。 |
| 没有悬浮聊天机器人气泡 | 通过 | 应用壳仅渲染 `RouterView`；回归测试确认不存在旧 AI shell。 |

## 8. 可访问性

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 全流程可使用键盘完成 | 通过 | 重构页只包含原生路由链接；无自定义交易控件。 |
| 焦点始终可见且浮层关闭后恢复 | 通过 | 使用全局 `:focus-visible`；重构页无浮层。 |
| 表格、标签、对话框和常驻侧栏语义正确 | 不适用 | 已删除这些交易界面。 |
| 状态不只依靠颜色表达 | 通过 | 重构状态由文本标题与说明表达。 |
| 图表提供文字摘要 | 不适用 | 已删除图表。 |
| 支持 reduced motion | 通过 | 重构页没有持续动画；全局 reduced-motion 规则保留。 |

## 9. 工程验证

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 行情服务单元测试通过 | 不适用 | 前端行情服务已删除。 |
| 前端语法和静态检查通过 | 通过 | `npm run type-check`、`npm run lint` 无错误。 |
| 真实 PandaData 请求冒烟测试通过 | 不适用 | 重构期不得由浏览器请求市场数据。 |
| 1440、1024 和小于 1024 px 的代表宽度浏览器测试通过 | 不适用 | 当前没有交易工作区；重构页为单一自适应页面。 |
| 控制台无未处理错误 | 通过 | Vitest 回归测试通过，重构页装载测试无控制台错误。 |

## 10. 最终结论

- 验收结论：通过；交易产品前端已清空，旧入口统一导向受保护的重构页。
- 未通过项目：无。
- 已知限制：交易功能暂停，后端接口与持久化数据未删除。
- 复现命令或测试入口：`cd frontend && npm run test && npm run type-check && npm run lint && npm run build`
