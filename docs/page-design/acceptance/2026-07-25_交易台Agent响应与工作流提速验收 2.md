# 交易台 Agent 响应与工作流提速验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_交易台Agent响应与工作流提速设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `pages/交易台.md` 已补充 revision 长轮询与真实阶段文案 |
| 真实数据、时间与失败状态符合合同 | 通过 | progress 长轮询只读取持久 Run/audit；错误不回退 |
| 主操作及权限边界正确 | 通过 | 服务端固定 mode/workflow，动作仍经目录、参数与 context_version 校验 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 单次结构化 turn；Agent phase、Evidence 与质量门未减少 |
| 设置隔离与提醒留存 | 不适用 | 本次未修改设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 失败 | 后端定向测试与前端 type-check/build 通过；既有前端消息测试受当前实时语音界面改动影响失败，浏览器烟测未完成 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 不适用 | 未改样式 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 未改顶栏 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 未改导航 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 未改壳层 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 未改壳层 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 未改行情展示 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 未改行情 |
| 仿真账户/仓位/订单有「仿真」标注 | 不适用 | 未改交易展示 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 状态来自 Run/progress；快捷指令逻辑未改变 |
| 「我的」/提醒/设置不进入 Agent 能力 | 通过 | 结构化 turn 只接收既有安全动作目录 |
| <1024px 同路由自适应，无独立移动流 | 不适用 | 未改响应式布局 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 不适用 | 未改交互控件或动效 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 活动 Run 禁止覆盖并可在刷新后恢复 | 通过 | `ACTIVE_WORKFLOW_STATUSES` 与 sessionStorage 恢复逻辑保留 |
| 终态证据或错误可读 | 通过 | 终态先显示 Evidence，快捷指令异步刷新不阻塞报告 |
| 工作流进度不由浏览器模拟 | 通过 | 单一 revision 长轮询读取服务端状态 |
