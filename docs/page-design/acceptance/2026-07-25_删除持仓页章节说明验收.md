# 删除持仓页章节说明验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `docs/page-design/briefs/2026-07-25_删除持仓页章节说明设计简报.md` 记录仅删除章节说明。 |
| 受影响路由与页面规范已更新 | 不适用 | `/desk` 页面规范未要求该说明文案，数据与版式规范未改变。 |
| 真实数据、时间与失败状态符合合同 | 通过 | 未修改数据逻辑；具体持仓仍保留行情元数据、不可用单元格与失败提示。 |
| 主操作及权限边界正确 | 通过 | 刷新与建账操作未修改。 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 未修改 Agent。 |
| 设置隔离与提醒留存 | 不适用 | 未修改设置或提醒。 |
| 单元测试、类型检查、构建和烟测 | 失败 | 类型检查、生产构建和 `/desk` 浏览器烟测通过；`rebuilding.spec.ts` 为 23 通过、8 失败，失败集中在当前工作树既有的标的代码/上下文断言及 `.instrument-select` 结构差异，与本次静态文案删除无关。 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 未修改样式和令牌。 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 未修改顶栏。 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 未修改工作区标签。 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 未修改壳层。 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 未修改 Agent 或布局。 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 未修改涨跌展示。 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 持仓表的 `quoteMeta` 与不可用状态未修改。 |
| 仿真账户/仓位/订单有「仿真」标注 | 通过 | “仿真账户”“仿真持仓”标题及相关状态文案仍保留。 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 未修改 Agent。 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 未修改相关能力。 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 仅删除静态段落，不改变响应式布局。 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 未增删交互控件；工作区标题和分区语义保持完整。 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 持仓数据仍明确区分仿真账本与真实行情 | 通过 | 账户/持仓标题保留“仿真”，行情缺失时继续显示具体不可用状态。 |
| 标注的章节说明不再渲染 | 通过 | 刷新 `http://localhost:3000/desk` 后精确文本定位数量为 0，持仓一级标题定位数量为 1。 |
