# 历史演示候选状态验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_历史演示候选状态设计简报.md` |
| 受影响路由与页面规范已更新 | 不适用 | 未改变 `/desk` 页面结构或数据合同 |
| 真实数据、时间与失败状态符合合同 | 通过 | 历史限制进入 `candidateNotice`；真实错误继续进入 `candidateError` |
| 主操作及权限边界正确 | 通过 | 历史演示仍不展示候选与候选操作 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 未改动 Agent |
| 设置隔离与提醒留存 | 不适用 | 未改动设置与提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | 137 项 Vitest、`vue-tsc --noEmit`、Vite build 均通过 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 复用 `.empty-data` |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 未改动 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 未改动 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 未改动 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 未改动 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 未涉及涨跌 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 未改动行情 |
| 模拟账户/仓位/订单有「模拟」标注 | 不适用 | 未改动 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 未改动 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 未改动 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 未新增布局分支 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 预期限制使用 `role="status"`，真实错误保留 `role="alert"` |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 历史演示不引入未来候选信息 | 通过 | `TradingDeskView.vue` 在模拟时钟存在时传入空候选和状态说明 |
| 预期不可用不误报为读取失败 | 通过 | `WatchlistWorkspace.vue` 独立渲染 `candidateNotice` |
