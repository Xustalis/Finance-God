# 教育画像候选生成验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `docs/page-design/briefs/2026-07-25_教育画像候选生成设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `/desk` 页面规范无需改变结构；`02_前后端职责与数据合同.md` 已明确教育限制不阻断候选 |
| 真实数据、时间与失败状态符合合同 | 通过 | 仍由候选服务读取画像方向、仿真持仓与 PandaData；无方向、无支持标的和行情失败分支未改 |
| 主操作及权限边界正确 | 通过 | 仍仅支持忽略候选与申请研究型交易计划，不直接下单 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 本次未改 Agent |
| 设置隔离与提醒留存 | 不适用 | 本次未改设置与提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端候选测试 7/7；`vue-tsc --noEmit` 通过；Vite 生产构建通过；ESLint 0 errors（25 个既有 warnings） |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 不适用 | 无视觉改动 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 无顶栏改动 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 无导航改动 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 无壳层改动 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 无 Agent 或布局改动 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 无行情呈现改动 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 候选服务的 PandaData 行情与显式失败分支保持不变 |
| 仿真账户/仓位/订单有「仿真」标注 | 不适用 | 无仿真事实呈现改动 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 无 Agent 改动 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 无能力目录改动 |
| <1024px 同路由自适应，无独立移动流 | 不适用 | 无响应式改动 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 不适用 | 无 DOM 或交互结构改动 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 可研究候选由服务端生成并显示画像版本、方向、行情时间和 Evidence | 通过 | `candidates_for_profile` 不再因 `education_only` 早退，其他生成与展示合同未改 |
| 候选不能直接下单 | 通过 | `WatchlistWorkspace` 操作保持“暂不研究 / 申请交易计划” |
| 候选不可用状态明确且不伪造默认推荐 | 通过 | 保留 `PROFILE_REQUIRED`、`PROFILE_DIRECTIONS_REQUIRED`、`NO_SUPPORTED_DIRECTION_CANDIDATES`、`MARKET_DATA_UNAVAILABLE` |

## 补充验证记录

`src/tests/rebuilding.spec.ts` 当前 31 项中 23 项通过、8 项失败；失败集中于既有测试预期 `000001.SZ` 与运行态 `000001.SH` 不一致，以及测试查询已不存在的 `.instrument-select`，不经过本次修改的候选教育模式分支。
