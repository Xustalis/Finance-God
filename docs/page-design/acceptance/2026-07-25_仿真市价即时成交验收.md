# 仿真市价即时成交验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_仿真市价即时成交设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `pages/交易台.md` 交易合同 |
| 真实数据、时间与失败状态符合合同 | 通过 | 服务端只绑定 PandaData 可信行情；组件展示时间、频率与 freshness |
| 主操作及权限边界正确 | 通过 | 仅用户点击可调用 `/simulation/market-orders`；Agent 只预填 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 未改变 Agent 合同 |
| 设置隔离与提醒留存 | 不适用 | 未改变 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端 17 项定向测试、前端 3 项定向测试、类型检查、Lint、生产构建；Playwright 打开 `/desk?preview=1` 并进入交易区 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 复用既有工作区样式 |
| 顶栏、工作区标签、双栏与 Agent 保持规范 | 通过 | 壳层未改动 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | `TradingWorkspace.vue` 真实行情区 |
| 仿真账户/仓位/订单有「仿真」标注 | 通过 | 标题与最近成交元数据 |
| &lt;1024px 同路由自适应，无独立移动流 | 通过 | 未改变 `/desk` 响应式壳层 |
| 键盘焦点、语义结构、reduced motion | 通过 | 原生表单、按钮和语义表格；未新增动效 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 仅保留标的、方向、数量和一个提交动作 | 通过 | `TradingWorkspace.vue` |
| 市价交易一次提交后直接返回 filled | 通过 | execution/API/e2e 测试 |
| 成功后刷新账户、持仓、订单与成交 | 通过 | `submitMarketOrder` 调用 `loadSimulationData` |
| 不显示草稿、复核、确认、撤单或对账 | 通过 | 交易组件已移除相应界面 |
| 幂等、余额不足与持仓不足不会产生重复或半成品成交 | 通过 | execution 与 e2e 测试 |
