# Agent 任务范围透明化最小完善验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `docs/page-design/briefs/2026-07-25_Agent任务范围透明化最小完善设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | 仅完善 `/desk` 已有 Agent 合同，未改变页面结构与数据合同；实现符合 `pages/交易台.md` |
| 真实数据、时间与失败状态符合合同 | 通过 | 范围摘要直接读取 `selectedQuote` 的 frequency/freshness/provider_time；缺失显示不可用 |
| 主操作及权限边界正确 | 通过 | 提交启用逻辑未变；范围区明确白名单动作和订单/撤单/资金划转禁令 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 固定上下文字段与三条快捷指令在真实浏览器出现；直答预览、真实直答、正式工作流预览均通过 |
| 设置隔离与提醒留存 | 通过 | 明示设置与凭据不可读；提醒实现未改动 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端相关测试 30/30；前端完整测试 116/116；类型检查、生产构建通过；真实浏览器完成预览、直答、历史与 900px 烟测 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 新区域仅使用现有 `--ink`、`--muted-ink`、`--rule`、`--faint-rule` |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 通过 | 顶栏未改动 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 通过 | 左侧导航未改动 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 通过 | 壳层未改动 |
| Agent 宽度可调（约 32%–60%）或有布局菜单等价入口 | 通过 | 布局逻辑未改动 |
| Agent 收起后可见 rail（非模态/气泡） | 通过 | rail 未改动 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 新区域不展示涨跌 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 范围摘要展示三项元数据与显式不可用态 |
| 仿真账户/仓位/订单有「仿真」标注 | 不适用 | 相关工作区未改动 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 既有测试与模板结构保持 |
| 「我的」/提醒/设置不进入 Agent 能力 | 通过 | 新说明强化该边界 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 新区域在 680px 以下改为单列，路由未变 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 使用语义 `section` 与 `dl`，无新增动画或交互焦点 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 用户在提交前可确认对象、数据质量、画像、执行方式、角色与产物 | 通过 | 输入“什么是市盈率？”后、发送前显示“直接回答 / 对话 Agent / 文字答复”；输入研究意图后显示“正式工作流 · 公司研究”及角色、`ResearchMemo` |
| 不新增第二事实源或本地伪造历史 | 通过 | 仅组合现有 Pinia 服务端状态，无新增持久化 |
| 1440px 真实页面固定展示 | 通过 | Playwright 访问真实 `/desk`；截图 `output/playwright/desk-context-1440.png` 显示固定上下文区与行情元数据 |
| 标的、工作区和组合联动 | 通过 | 真实点击 `600519.SH` 后标的更新；切换“持仓”后工作区更新为“持仓”，组合更新为真实仿真账户 ID |
| 900px 同路由自适应 | 通过 | Playwright 将视口调整为 900×1000；截图 `output/playwright/desk-context-900-current.png`，同一路由与固定上下文保持可读 |
| 直接回答决策后状态 | 通过 | 真实提交“什么是市盈率？”；`/api/agent/desk/decision/stream` 返回 200，页面展示完整回答且不再触发供应商流式 SDK 异常 |
| 正式工作流提交前预览 | 通过 | 输入“研究贵州茅台的基本面与估值”但未提交；`/api/agent/desk/preview` 返回 200，页面显示公司研究、去重角色及 `ResearchMemo` |
| 任务历史运行链路 | 通过 | 点击“任务历史”，`/api/workflows?limit=20` 返回 200 并展示真实服务端记录 |
