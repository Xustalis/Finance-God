# StepAudio 实时语音交互验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_StepAudio实时语音交互设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | 统一规范和交易台页面规范包含 Live 通话约束 |
| 真实数据、时间与失败状态符合合同 | 通过 | WebSocket 只映射白名单上游事件；代理、超时和服务端错误均显式返回文字模式 |
| 主操作及权限边界正确 | 通过 | 用户手动发起；后端认证用户并校验 onboarding 所有权或 desk context |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 语音系统指令禁止伪造服务端事实；既有工作流和 UI action 路径未替换 |
| 设置隔离与提醒留存 | 通过 | API Key 仅由后端读取；浏览器首帧只发送用户 Token 和业务上下文 |
| 单元测试、类型检查、构建和烟测 | 通过 | 前端连接状态测试 8 项、后端实时语音测试 3 项、部署配置检查、type-check、lint、Vite build 均通过；经 Vite 同源代理收到真实 `session.ready` |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | `.live-call` 使用现有 paper/ink/rule 令牌 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 未修改交易台顶栏 |
| 工作区横向标签；当前朱红底边 | 不适用 | 未修改工作区标签 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 通过 | Live 状态位于现有右侧 Agent 栏 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 通过 | 通话原位替换线程与输入，不使用弹层 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 通话界面不展示涨跌 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 未修改行情区域 |
| 仿真账户/仓位/订单有「仿真」标注 | 不适用 | 未修改仿真业务展示 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 文字模式保留既有实现；通话结束后恢复 |
| 「我的」/提醒/设置不进入 Agent 能力 | 通过 | Realtime 会话未注入这些能力 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 680px 样式仅收缩同一状态板 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | button、aria-live、语义 section；动画只在允许动效时启用 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 通话位于 Agent 右栏且左侧工作区保持挂载 | 通过 | `DeskAgentPanel.vue` 内部状态切换 |
| 用户开口打断 AI 播报 | 通过 | `speech.started` 发送 `response.cancel` 并关闭当前输出 AudioContext |
| 故障保留文字模式 | 通过 | 握手 `error/close/timeout` 与 `session.error/session.closed` 均清理资源；服务端消息及错误码不会被通用提示覆盖 |
| 原始音频不落盘 | 通过 | 后端只转发音频；仅最终 onboarding 字幕写入 `ProfileMessage` |
| 开发与生产代理支持 WebSocket | 通过 | Vite 启用 `ws: true`；两层 Nginx 转发 `Upgrade` 与 `Connection`；部署检查包含静态断言 |
| 失败后可重试 | 通过 | composable 回到 `error` 可启动态；自动测试验证第二次连接成功 |
