# AI 消息纸张与报刊排版验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_AI消息纸张与报刊排版设计简报.md` 记录纸片材质、报刊题签与响应式范围 |
| 受影响路由与页面规范已更新 | 通过 | `pages/交易台.md` 的 Agent 壳层规格已补充纸片消息与字体层级 |
| 真实数据、时间与失败状态符合合同 | 不适用 | 本次仅修改 CSS 与页面视觉说明，不修改消息数据或状态渲染 |
| 主操作及权限边界正确 | 不适用 | 提交、查询、展开和权限逻辑未改动 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | `DeskAgentPanel.vue` 未改动；`desk-agent-messages.spec.ts` 通过 |
| 设置隔离与提醒留存 | 不适用 | 本次不涉及设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | Vitest 97/97；`vue-tsc --noEmit` 通过；ESLint 0 error（9 条既有 warning）；Vite production build 通过 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 消息使用 `--paper-light`、`--paper`、`--ink`、`--rule`，无新增蓝色、渐变或圆角 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 顶栏未改动 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 工作区导航未改动 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 壳层布局未改动 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 通过 | 只调整 `.agent-chat-message`，未改变 Agent 显示或壳层入口 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 本次不涉及行情语义 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 本次不涉及行情 |
| 仿真账户/仓位/订单有「仿真」标注 | 不适用 | 本次不修改业务文案 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 快捷指令与工作流 DOM/逻辑未改动；相关消息测试通过 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 本次不涉及能力目录 |
| `<1024px` 同路由自适应，无独立移动流 | 通过 | 保留原响应式壳层；`≤680px` 仅缩小消息纸片内边距与阴影 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 无交互 DOM 或动效变更；现有焦点与语义结构保持 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| Agent 纸片消息能清楚区分用户与 AI | 通过 | 用户为右侧墨色订线并右对齐，AI 为左侧朱红订线；身份题签同时提供文字信号 |
| 普通回复、任务、研究报告与仿真回执共享同一纸张语言 | 通过 | 普通气泡与多功能消息统一浅纸底、细墨边、偏移硬阴影和直角折角 |
| 字体与层级符合报刊阅读 | 通过 | 正文显式使用 `--font-serif`，题签使用 `--font-numeric`、双线与扩展字距；长文提高行距并启用中文两端对齐 |
| 宽屏可拖分栏；收起后 rail 仍可见 | 不适用 | 当前统一规范明确左右固定等宽且 Agent 始终展开，本次不恢复旧布局控制 |
