# 复盘本地演示数据验收

| 验收项 | 结果 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_复盘本地演示数据设计简报.md` |
| 仅本地显式预览入口启用 mock | 通过 | `TradingDeskView.vue` 同时要求 `import.meta.env.DEV` 与 `route.query.preview === '1'` |
| 真实案例优先于 mock | 通过 | `ReviewWorkspace.vue` 要求案例为空才展示演示数据 |
| 接口失败不被 mock 掩盖 | 通过 | `showMock` 要求 `error` 为空；错误仍使用 `role="alert"` |
| 演示数据有明确披露 | 通过 | 标题下显示“演示数据”，并声明不代表真实账户、成交或 Agent 结论 |
| 覆盖完成与进行中状态 | 通过 | 600519.SH 为已完成终局复盘；510300.SH 为持仓进行中 |
| 模拟账户/仓位/订单有「模拟」标注 | 通过 | 页面章标题保留“模拟交易”，演示披露进一步限定数据性质 |
| 未引入卡片矩阵、蓝色强调或独立移动流 | 通过 | 复用现有表格、时间线、规则线与响应式壳层 |
| 键盘可选择案例 | 通过 | 表格行保留 `tabindex=0` 与 Enter 选择 |
| 定向单元测试 | 通过 | `vitest run --run src/tests/review-workspace.spec.ts`，8 项通过 |
| TypeScript 类型检查 | 通过 | `npm run build` 内置 `vue-tsc --noEmit` 通过 |
| 生产构建 | 通过 | `vite build` 完成，1686 个模块转换成功 |
