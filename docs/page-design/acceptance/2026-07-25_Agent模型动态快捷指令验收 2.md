# Agent 模型动态快捷指令验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_Agent模型动态快捷指令设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | 更新统一规范、前后端合同与 `pages/交易台.md` |
| 真实数据、时间与失败状态符合合同 | 通过 | 模型失败返回空列表及 `quick_commands_error`，无静态兜底 |
| 主操作及权限边界正确 | 通过 | 生成提示仅允许研究、解释、比较、核查及仿真草稿，不允许代下单 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 模型输入仅为 section、symbol、脱敏画像；提交入口与工作流边界未改 |
| 设置隔离与提醒留存 | 通过 | 模型输入不含设置、凭据或提醒；相关代码未改 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端 desk/runtime 22 项、前端 rebuilding 20 项、vue-tsc、Vite build |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 未改令牌和布局 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 未改顶栏 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 未改工作区标签 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 未改壳层 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 通过 | 仅更新输入区内容 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 未改行情 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 未改行情 |
| 仿真账户/仓位/订单有「仿真」标注 | 不适用 | 未改交易数据 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 模型成功严格校验三条；失败明确显示，不伪造 |
| 「我的」/提醒/设置不进入 Agent 能力 | 通过 | 仅传脱敏画像投影 |
| <1024px 同路由自适应，无独立移动流 | 不适用 | 未改响应式布局 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 指令仍为 button；错误状态使用 `role=status` |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 工作区或标的变化生成新的上下文指令 | 通过 | bootstrap provider 接收 section、symbol 与 projection |
| 不使用固定或模板化指令兜底 | 通过 | 删除 `_QUICK_COMMANDS` 与前端 computed 固定映射 |
