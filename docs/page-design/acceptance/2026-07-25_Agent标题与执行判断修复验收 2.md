# Agent 标题与执行判断修复验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_Agent标题与执行判断修复设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | 统一规范和 `pages/交易台.md` 已同步标题栏与执行说明合同 |
| 真实数据、时间与失败状态符合合同 | 通过 | PandaData 历史波动率使用完整窗口；空数据仍显式失败 |
| 主操作及权限边界正确 | 通过 | 服务端批准 `answer | workflow` 路径；前端不展示内部判断说明，也不提供布局设置 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 前端不展示路由原因；简单消息走对话 Agent，复杂任务仍走持久化工作流 |
| 设置隔离与提醒留存 | 不适用 | 本次未改设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | `rebuilding.spec.ts` 20/20；`npm run type-check`、`npm run build` 通过；真实浏览器完成问候发送与回复 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 未新增令牌或表面 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 顶栏未改 |
| 工作区横向标签；当前朱红底边 | 不适用 | 工作区标签未改 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 通过 | 页面结构未改 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 通过 | 浏览器确认布局入口、分隔拖拽和 rail 均为 0，Agent 区为 1 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 本次未改行情视觉 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 量化监测只使用真实 PandaData 数据 |
| 仿真账户/仓位/订单有「仿真」标注 | 不适用 | 本次未改仿真业务区 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 快捷指令未改，任务状态仍来自服务端 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 本次未改能力目录 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 787px 真实浏览器保持 `/desk`，左侧工作区和 Agent 同页上下排列 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 输入框、发送按钮、导航与 Agent `aria-live` 语义保留 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 标题区不显示内部 context、“重置布局”或“收起” | 通过 | 787px 浏览器 DOM 仅保留 `AI AGENT` 标题 |
| 不显示静态能力说明或“本次任务范围” | 通过 | 浏览器 DOM 中 ready/context 节点均为 0 |
| 简单消息获得真实对话 Agent 回复并以气泡展示 | 通过 | 实际点击发送“你好”；Ark 两次 `/api/v3/responses` 200，`/api/agent/desk/decision` 200；用户与 Agent 气泡均可见，路由说明为 0 |
| 市场状态监测不再因周末单日波动率查询失败 | 通过 | PandaData 完整窗口返回 21 条历史波动率；实际 WorkflowRun `workflow-e51fda2452f94ff0873880a122c9db19` 中量化节点首轮完成，最终 12/12 完成 |
| 不显示工作区布局设置 | 通过 | 浏览器确认 `[aria-label="调整工作区布局"]`、`.layout-menu`、`.desk-resizer` 与 `.agent-rail` 数量均为 0 |
