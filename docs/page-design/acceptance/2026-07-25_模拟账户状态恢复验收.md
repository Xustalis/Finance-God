# 模拟账户状态恢复验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `docs/page-design/briefs/2026-07-25_模拟账户状态恢复设计简报.md` |
| 受影响路由与页面规范已更新 | 不适用 | 路由、布局与页面行为规范未变，仅收窄账户/时钟数据合同 |
| 真实数据、时间与失败状态符合合同 | 通过 | 账户存在性与可空 `simulation_time` 分离；服务错误仍保留显式错态 |
| 主操作及权限边界正确 | 通过 | 只有明确无账户时才开放建户 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 未改动 |
| 设置隔离与提醒留存 | 不适用 | 未改动 |
| 单元测试、类型检查、构建和烟测 | 失败 | 时钟定向测试与前端类型检查通过；完整 e2e 暴露历史时钟下订单有效期的既有失败，页面烟测受本地认证接口超时阻断 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 不适用 | 无视觉改动 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 无改动 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 无改动 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 无改动 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 无改动 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 无改动 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 无行情改动 |
| 模拟账户/仓位/订单有「模拟」标注 | 通过 | 沿用 `PortfolioWorkspace.vue` 现有文案 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 无改动 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 无改动 |
| <1024px 同路由自适应，无独立移动流 | 不适用 | 无布局改动 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 不适用 | 无交互改动 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 账户读取失败不伪造成“无账户” | 通过 | 前端账户状态机保持 `error`；后端仅把缺少时钟表示为 `simulation_time: null` |
| 既有账户缺少时钟时仍可读取账户事实 | 通过 | `SimulationClockService.find_for_account` 与账户装配逻辑 |
