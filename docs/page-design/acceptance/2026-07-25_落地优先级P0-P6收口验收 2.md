# 落地优先级 P0–P6 收口验收

| 项目 | 状态 | 证据 |
|---|---|---|
| P0 索引/默认能力诚实 | 通过 | `docs/项目索引.md`、`README.md` Current desk capabilities；产品约束：真行情 + 仿真账户/订单、Agent 不提交/撤单/改设置/DOM、失败标 stale/error、WorkflowRun 为任务真相、Toast≠已读 |
| P1 Desk Bootstrap + 画像投影 + UiAction 目录 | 通过 | `GET /desk/bootstrap` 裸 JSON；`project_suitability_profile` 剥离 `objective_profile`/`dimension_scores`/问卷；`SAFE_UI_ACTIONS` v1；`tests/api/test_desk_bootstrap.py` |
| P4 Workflow Worker（最高产品阻塞） | 通过 | 进程内 + `run_workflow_worker.py`；`queued→` 终态；`tests/workflows/test_workflow_worker.py`；`capabilities.workflow_worker=true` |
| P2 行情可观察 | 通过 | `MarketPoller` + `GET /market/snapshots`、`GET /market/alerts`；失败不伪造价格 |
| P3 提醒历史 | 通过 | `GET /workspace/notifications/history`；`NotificationRepository.list_history`；`test_notification_history_includes_read_and_honors_filters` |
| P5/P6 Agent 路由 + UI 动作回执 | 通过 | Agent 仅注入投影；`POST /desk/ui-actions` 回执 `applied\|rejected\|stale_context`；FE `applyUiAction` 客户端目录门禁 + 仅安全副作用 |
| 前端接线 | 通过 | `stores/tradingDesk.ts`：`profileProjection`、`loadNotificationHistory`、`applyUiAction`、工作流 2s 轮询；`DeskAgentPanel` 文案与 Worker 一致 |
| 后端契约测试 | 通过 | `pytest tests/api/test_desk_bootstrap.py tests/api/test_agent_routes.py tests/workflows/test_workflow_worker.py tests/api/test_workspace_routes.py` → **21 passed**（2026-07-25） |

## 明确未上线（不得写成已可用）

- `GET /workflows/{run_id}/events` 节点事件流
- `POST /workflows/{run_id}/cancel`
- 多 Worker `SKIP LOCKED` 租约与 Outbox/SSE
- Agent 代客提交/撤单/改设置/DOM 操作

## 产品约束复查

| 约束 | 结果 |
|---|---|
| PandaData 凭据不进浏览器 | 保持：仅服务端适配 |
| 行情真实；账户/订单/执行标仿真 | 保持 |
| Agent 不提交/撤单/设置/DOM | 保持：目录无对应动作；FE 不应用 |
| 失败显示 stale/error，不伪造行情 | 保持 |
| 一个 WorkflowRun 为正式任务真相 | 保持：面板创建并轮询 |
| Toast 关闭 ≠ 已读 ≠ 已处理 | 保持：历史接口只暴露 unread\|read |
