# 四个交易台工作区验收

| 项目 | 状态（通过/失败/不适用） | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_交易台总览版式简报.md` 与 `TradingDeskView.vue`。 |
| 主画布与响应式要求 | 通过 | `styles.css`：1440px 等宽双栏/双线，1023px 以下同路由上下布局。 |
| 真实数据、时间与失败状态 | 通过 | `tradingDesk.ts` 共享行情缓存，并为披露事实带齐季度参数；四个工作区显式展示上游时间、空状态或请求错误，不生成替代行情。 |
| 主操作及权限边界 | 通过 | 仿真账户、分组、候选忽略和订单完整确认链路均由服务端接口执行；设置和最终提交不属于 Agent。 |
| Agent 上下文、快捷指令与工作流 | 通过 | `DeskAgentPanel.vue` 显示 section/symbol/contextVersion、三条指令及后端 run ID/status。 |
| 设置隔离与提醒留存 | 失败 | 设置与 Agent 已隔离；但现有 `/workspace/notifications` 仅返回未读提醒，后端没有可浏览的已读历史合同，前端不能伪造“提醒留存”。 |
| 键盘、焦点、语义与减弱动效 | 通过 | 工作区使用语义导航、表格、表单与 details；全局焦点/`prefers-reduced-motion` 规则仍有效。 |
| 单元测试、类型检查、构建和烟测 | 通过 | `npm run type-check`、`npm run test -- --run`（59 项）及 `npm run build` 通过；组件测试挂载 `/desk` 并验证常驻 Agent。 |
| 测试账户前后端联调 | 失败 | 账户/持仓/订单/自选/候选均 200；自选新增、移除、删除及候选忽略、恢复和 Workflow 创建均成功。2026-07-25（周六）行情接口因 `closed_pending` 返回 `MARKET_DATA_EMPTY_RESPONSE`，因此没有真实引用价，订单草稿至提交不能在该时段完成。 |
