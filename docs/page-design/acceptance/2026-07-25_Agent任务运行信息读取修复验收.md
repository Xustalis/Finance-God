# Agent 任务运行信息读取修复验收

| 项目 | 状态 | 证据 |
|---|---|---|
| 任务气泡按 `run_id` 读取服务端事实 | 通过 | `DeskAgentPanel.vue` 不再依赖历史分页查找 Run |
| Run 与 progress 保持同一服务端来源 | 通过 | `openHistoricalWorkflow` 并行读取两个正式接口 |
| 读取失败明确可见且无本地假状态 | 通过 | store 保存真实请求错误并清空旧详情 |
| 纸面/墨色、双栏和 Agent 视觉不变 | 不适用 | 本次无样式或 DOM 结构变更 |
| 行情、模拟业务与设置隔离 | 不适用 | 本次不触及相关合同 |
| 1440px 与低于 1024px 布局 | 不适用 | 本次无布局变更 |
| 键盘、语义结构与减弱动效 | 不适用 | 本次无交互控件或动效变更 |
| 定向测试 | 通过 | Agent 消息组件与 store 定向测试 |
