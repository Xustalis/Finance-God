# Agent 自学习复盘展示验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_Agent自学习复盘展示设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `/desk` 交易台页面规范与统一设计规范已补充 |
| 真实数据、时间与失败状态符合合同 | 通过 | 摘要接口只读原子快照与周期文件，区分健康、陈旧、未运行和损坏 |
| 主操作及权限边界正确 | 通过 | 鉴权只读；浏览器不能启动 Worker 或修改知识 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 未修改右侧 Agent 交互 |
| 设置隔离与提醒留存 | 不适用 | 未改动 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端目标回归 12 项、前端组件与联动 40 项通过；`vue-tsc --noEmit`、Vite build、Ruff、compileall、Compose 服务静态解析与生产配置检查通过；Playwright 打开生产预览并正常进入登录路由（仅既有 favicon 404） |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 复用 `.overview-section`、定义列表和规则线 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 壳层未改动 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 导航未改动 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 壳层未改动 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 壳层未改动 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 自学习区不展示涨跌 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 自学习区不展示行情报价 |
| 模拟账户/仓位/订单有「模拟」标注 | 不适用 | 原复盘标注保持不变 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 右侧 Agent 未改动 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 未改动 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 复用复盘工作区流式布局 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 语义分区、列表与按钮；无新增动效 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| Worker 状态、最近周期和知识版本来自真实后端 | 通过 | `GET /api/agent-learning/summary` |
| 只展示最多五条已验证成果 | 通过 | 服务端过滤并限制数量 |
| 未运行、陈旧、损坏、空成果与接口失败可区分 | 通过 | 后端状态合同与前端独立错误/空态 |
| 自学习失败不影响交易案例复盘 | 通过 | Store 使用独立 loading/error 状态 |
