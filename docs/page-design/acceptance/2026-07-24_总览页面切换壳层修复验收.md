# 总览页面切换壳层修复 — 前端验收

> 日期：2026-07-24  
> 范围：`/markets` 切换至 `/overview` 后的应用壳层布局

## 1. 任务与信息架构

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 页面首屏能识别用户任务、当前对象和主要状态 | 通过 | 1440 px 浏览器实测报头、总览标签和市场主体依次可见 |
| 每个区域只有一个明确职责 | 通过 | 根网格仅包含 `masthead`、`tabs`、`body` 三个命名区域 |
| 主操作唯一且结果可预测 | 不适用 | 本次只修复栏目切换后的壳层定位，不改页面操作 |
| 页面与上一步、下一步对象保持连续 | 通过 | 从 `/markets` 点击“总览”进入 `/overview`，未重载或修改 market store |
| 没有无功能价值的区域或卡片 | 不适用 | 本次未改变总览内容结构 |

## 2. 文案

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 标题直接说明区域内容或操作 | 不适用 | 未修改文案 |
| 辅助文字说明范围、操作、时效或决策价值 | 不适用 | 未修改文案 |
| 删除了口号、隐喻、设计说明和重复文案 | 不适用 | 未修改文案 |
| 按钮使用“动词 + 对象/结果” | 不适用 | 未修改按钮 |
| 错误、空状态和恢复条件表达明确 | 不适用 | 未修改状态文案 |

## 3. 真实数据

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 市场数据来自 PandaData，不含硬编码行情 | 不适用 | 未修改数据接入 |
| 显示来源、供应商时间、实际频率和新鲜度 | 不适用 | 未修改数据状态 |
| 页面刷新间隔与数据频率分别表达 | 不适用 | 未修改刷新控制 |
| 请求失败不会回退到模拟行情 | 通过 | 浏览器冒烟中接口未配置时显示“连接失败”，未出现模拟行情 |
| 真实行情与仿真账户/订单明确分离 | 不适用 | 未修改业务数据 |
| 浏览器不可访问 PandaData 凭据 | 不适用 | 未修改凭据边界 |

## 4. 工作区与桌面适配

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 1440 px 下信息层级和密度符合规范 | 通过 | 实测报头 `top=10,height=64`，标签 `top=74`，主体 `top=116.14` |
| 1024 px 下主要任务仍可完成且无页面横向溢出 | 通过 | 1024×768 实测 `scrollWidth=1024`，报头、标签、主体顺序稳定 |
| 小于 1024 px 显示桌面端提示 | 不适用 | 本次未改变全局最低宽度策略 |
| 没有移动导航或移动页面 | 通过 | 修复只使用桌面根网格，不新增移动导航 |
| 相关面板可显示、隐藏和调整尺寸 | 不适用 | 未修改面板能力 |
| 工作区布局可以保存、恢复和重置 | 不适用 | 未修改布局偏好 |

## 5. 视觉

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 使用暖米色报纸终端、衬线字体和紧凑专业工作区视觉 | 通过 | 修复沿用现有设计 Token 与页面表面 |
| 页面没有卡片墙、装饰渐变或常规阴影 | 不适用 | 未修改页面视觉组件 |
| 常规强调、状态、边框和装饰未使用蓝色 | 不适用 | 未修改颜色 |
| 红绿只用于交易、损益和状态语义 | 不适用 | 未修改颜色语义 |
| 数字精度、单位、对齐和时区一致 | 不适用 | 未修改数据呈现 |

## 6. 状态与交互

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 加载、正常、空、延迟、过期和失败状态已覆盖 | 不适用 | 未修改状态系统 |
| 行情刷新不会清除选择或未提交输入 | 通过 | 修复仅改变 CSS Grid 声明 |
| 隐藏标签页停止轮询，恢复后立即刷新 | 不适用 | 未修改轮询控制 |
| 相同请求由统一控制器去重 | 不适用 | 未修改请求控制 |
| 仿真订单状态未知时不能重复提交 | 不适用 | 未修改订单 |

## 7. AI

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| AI 分析绑定当前对象和数据版本 | 不适用 | 未修改 AI 数据 |
| 显示支持证据、反方证据和未知项 | 不适用 | 未修改 AI 内容 |
| AI 不直接修改或提交仿真订单 | 不适用 | 未修改 AI 操作 |
| 全部桌面路由常驻同一个 AI 侧栏 | 不适用 | 本次只修复总览壳层定位 |
| 侧栏可收起为可见窄栏并恢复展开 | 不适用 | 未修改侧栏 |
| 展开/收起选择可跨路由和刷新恢复 | 不适用 | 未修改侧栏状态 |
| 1024–1279 px 首次访问默认收起 | 不适用 | 未修改侧栏默认值 |
| AI 侧栏不使用遮罩且不会遮挡主要操作 | 通过 | 1440 与 1024 px 实测主体从标签栏下方开始，无壳层遮挡 |
| 没有悬浮聊天机器人气泡 | 不适用 | 未修改 AI 入口 |

## 8. 可访问性

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 全流程可使用键盘完成 | 不适用 | 未修改交互控件 |
| 焦点始终可见且浮层关闭后恢复 | 不适用 | 未修改焦点或浮层 |
| 表格、标签、对话框和常驻侧栏语义正确 | 不适用 | 未修改语义结构 |
| 状态不只依靠颜色表达 | 不适用 | 未修改状态表达 |
| 图表提供文字摘要 | 不适用 | 未修改图表 |
| 支持 reduced motion | 通过 | 未引入动画，沿用全局 `prefers-reduced-motion` |

## 9. 工程验证

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 行情服务单元测试通过 | 不适用 | 未修改行情服务 |
| 前端语法和静态检查通过 | 通过 | `npm run type-check`、`npm run build` 通过；lint 0 error、9 个既有 warning |
| 真实 PandaData 请求冒烟测试通过 | 失败 | 本机接口返回未配置/400，页面正确显示失败状态 |
| 1440、1024 和小于 1024 px 浏览器测试通过 | 失败 | 1440 与 1024 通过；小于 1024 不在本次修复范围且未验证 |
| 控制台无未处理错误 | 失败 | 本机行情接口未配置，并存在既有空图表 `NaN` 控制台错误 |

## 10. 最终结论

- 验收结论：本次“报头切换后落到页面底部”修复通过；项目级全量验收未全部通过。
- 未通过项目：本机真实 PandaData 冒烟、小于 1024 px 全局提示、既有行情空数据控制台错误；另有既有 `desk-api-auth.spec.ts` fixture 失败。
- 已知限制：未处理与壳层定位无关的数据服务、图表空数据和订单测试问题。
- 复现命令或测试入口：`npm test -- --run src/tests/layout-contract.spec.ts`；浏览器从 `/markets` 点击“总览”。
