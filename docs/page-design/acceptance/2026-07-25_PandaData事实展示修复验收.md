# PandaData 事实展示修复验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `docs/page-design/briefs/2026-07-25_PandaData事实展示修复设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | 仅影响 `/desk`；现有 `pages/交易台.md` 已规定融资原始观察值与披露原始事实，无需修改规范 |
| 真实数据、时间与失败状态符合合同 | 通过 | 不改 API、来源戳或错误状态；只在展示层排除结构字段与空字段 |
| mock 仅用于获准的非交易只读模块，且模式、原因、时间、来源与恢复行为可验证 | 通过 | 既有 mock 披露回归 2 项通过；本次未改变 mock 分支 |
| 主操作及权限边界正确 | 不适用 | 未新增或修改操作 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 未改变 Agent 数据或行为 |
| 设置隔离与提醒留存 | 不适用 | 未改变设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 失败 | 新增定向测试 3/3、既有相关测试 2/2、改动文件 ESLint、Vite build 均通过；全量 `vue-tsc --noEmit` 被既有测试夹具缺少 `DeskQuote.trade_eligible` 的 8 个错误阻断 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 未修改样式与壳层 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 未修改顶栏 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 未修改导航 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 未修改布局 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 未修改 Agent |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 未修改行情 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 未修改行情合同 |
| mock 未进入价格、估值、Agent/Evidence、风控、订单、成交或审计 | 通过 | 本次仅格式化只读事实 DOM |
| 模拟账户/仓位/订单有「模拟」标注 | 不适用 | 未修改模拟业务 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 未修改 Agent |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 未修改能力目录 |
| `<1024px` 同路由自适应，无独立移动流 | 通过 | 未新增布局或路由，复用现有事实条响应式 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 未新增交互；保留现有语义结构 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 总览事实保留真实 PandaData 来源且无假值 | 通过 | 原始批次与来源戳不变；空财务指标显示明确空状态 |
| 融资余额不展示 `date`、`margin_type`、`symbol` 等结构字段 | 通过 | `overview-facts.spec.ts` 定向回归通过 |
| 公司披露优先展示非空业务字段 | 通过 | `overview-facts.spec.ts` 覆盖空字段在有效字段之前的响应 |
