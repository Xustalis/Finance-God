# Agent 多功能消息气泡验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_Agent多功能消息气泡设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `pages/交易台.md`、统一设计规范及数据合同已更新 |
| 真实数据、时间与失败状态符合合同 | 通过 | 报告仅由终态 Evidence 创建；草稿/回执仅由仿真接口成功响应创建；错误进入显式消息 |
| 主操作及权限边界正确 | 通过 | 草稿气泡只提供“前往复核”，最终提交仍在独立确认流 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 原有 bootstrap、decision、WorkflowRun 和 Evidence 路径保留 |
| 设置隔离与提醒留存 | 不适用 | 本次未改设置、提醒或其 Agent 能力边界 |
| 单元测试、类型检查、构建和烟测 | 通过 | Vitest 91/91；`vue-tsc --noEmit`；ESLint 0 errors；Vite build 通过 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 消息样式复用 `--paper`、`--ink`、`--risk` 与规则线 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 壳层未改 |
| 工作区横向标签；当前朱红底边 | 不适用 | 左侧导航未改 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 分栏未改 |
| Agent 宽度可调或有布局菜单等价入口 | 不适用 | 布局偏好未改 |
| Agent 收起后可见 rail | 不适用 | rail 未改 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 行情展示未改 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 行情展示未改 |
| 仿真账户/仓位/订单有「仿真」标注 | 通过 | 草稿与订单回执气泡标题均明确标注“仿真” |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 快捷指令结构保留；workflow 气泡读取真实 Run/progress |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 能力目录未改 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 仍使用 `/desk` 与现有响应式规则 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 使用 article、section、details、button、aria-live；沿用全局 reduced-motion |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 用户普通气泡与 AI 普通回复按时间展示 | 通过 | `AgentThreadMessage` + `DeskAgentPanel`；组件测试覆盖 |
| 研究报告位于 AI 气泡内部 | 通过 | `.is-research_report > .agent-result`；测试验证结论、风险和来源 |
| 仿真订单草稿位于 AI 气泡内部 | 通过 | `.is-order_draft > .agent-order-bubble`；测试验证字段、未提交说明与复核动作 |
| 普通回答不重复调用研究接口 | 通过 | `submitAgentIntent` 直接使用 decision `answer_text` |
| 刷新保留当前浏览器会话消息 | 通过 | `finance-god:agent-thread:v1` 保存最近 80 条消息；服务端事实仍按接口刷新 |
