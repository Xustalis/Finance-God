# Agent 主控交易台用户旅程原型验收

> 验收对象：`frontend/prototypes/journey-desk/`  
> 性质：隔离交互原型，不代表生产 `/desk` 已完成 Phase 1–8。

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `docs/page-design/briefs/2026-07-25_Agent主控交易台用户旅程原型设计简报.md`；实现保持四工作区、右栏 Agent 和设置隔离 |
| 受影响路由与页面规范已更新 | 不适用 | 原型运行在独立 Vite 端口，不注册或修改生产路由 |
| 真实数据、时间与失败状态符合合同 | 通过 | 只请求 `/api/market/snapshots`；1440px 烟测得到服务端 500 时页面显示“行情不可用…未使用演示价格回退” |
| 主操作及权限边界正确 | 通过 | 浏览器烟测中“填写 200 股”只写未提交草稿；提交按钮禁用；单测拒绝提交、撤单、资金划转 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 切换持仓后 context v1→v2 且恰好三条指令变化；当前请求明确标“未创建”；完成态明确标“静态合同示意”，未伪造运行 |
| 设置隔离与提醒留存 | 通过 | 浏览器烟测“打开用户设置”被 Agent 拒绝；顶部“我的”可由本人打开；关闭提醒浮层不自动改已读 |
| 单元测试、类型检查、构建和烟测 | 通过 | 本轮 Vitest 9/9、`vue-tsc`、独立 ESLint 配置与 Vite build 通过；相关后端 Desk/Worker/通知测试 25/25 通过；既有 Playwright 1440/900 烟测通过 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | `src/styles.css` 使用 paper/ink/rule；界面为规则线、表格、定义列表 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 通过 | 1440px 截图 `output/playwright/journey-desk-1440.png` |
| 工作区横向标签；当前朱红底边（`#c91418`） | 通过 | 总览/持仓/自选/交易横排；当前态使用 `--accent: #c91418` |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 通过 | 1440px 烟测和截图 |
| Agent 宽度可调（约 32%–60%）或有布局菜单等价入口 | 通过 | “布局”内 range 32–60，可恢复 1:1 |
| Agent 收起后可见 rail（非模态/气泡） | 通过 | `output/playwright/journey-desk-agent-rail.png` |
| 涨跌仅用正/风险色，且有非颜色信号 | 通过 | 表格同时输出带正负号百分比；颜色只附加语义 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 快照表字段齐全；后端失败烟测显示错误且无价格 |
| 仿真账户/仓位/订单有“仿真”标注 | 通过 | 持仓页、交易页、提交边界均显式标注 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 单测验证每区三条；原型不连接工作流时显示“未创建”，静态 completed 示例明确不代表真实运行 |
| “我的”/提醒/设置不进入 Agent 能力 | 通过 | `SAFE_ACTIONS` 单测不含设置/提交/撤单/划转；设置面板文案和浏览器拒绝回执 |
| `<1024px` 同路由自适应，无独立移动流 | 通过 | Playwright 900px 完整烟测；同一 DOM 改为上下排列，截图 `output/playwright/journey-desk-900.png` |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | `nav/table/form/details/aside/aria-live`；全局 `:focus-visible`；reduced-motion 媒体查询 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 顶栏仅字标 + 提醒 + 我的，无蓝链导航 | 通过 | 1440px 截图 |
| 四标签横向，当前朱红下划线 | 通过 | 浏览器切换与截图 |
| 宽屏可调分栏；收起后 rail 仍可见 | 通过 | 布局菜单 + rail 截图 |
| 总览表格有上游时间/频率/freshness；失败无假价 | 通过 | `MarketSnapshot` 合同与真实失败烟测 |
| 持仓/钱包/订单标注仿真 | 通过 | 原型不单列钱包；“我的”写“仿真资金”，持仓和交易页显式标注 |
| Agent 三条快捷指令 + 工作流状态边界 | 通过 | 动态三指令、未创建态和静态合同示例 |
| “我的”与提醒不进入 Agent 能力 | 通过 | 单测和浏览器烟测 |

## D. 已知环境状态

- 浏览器请求 `/api/market/snapshots` 收到后端 HTTP 500，页面按合同显示显式错误。这是后端当前运行环境/数据库链路的真实状态，不是原型回归。
- 后端已具备进程内/独立 Workflow Worker，并通过本轮相关自动化测试；由于隔离原型没有带认证的完整 PandaData/模型运行环境，本次仍未做真实研究 Run 终态浏览器烟测，不能据此宣称生产工作流和外部依赖完成。
