# 统一证券代码与测试合同修复验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | 同主题设计简报 |
| 受影响路由与页面规范已更新 | 不适用 | `/desk` 既有规范已明确默认 `000001.SZ` 与 UI action 合同 |
| 真实数据、时间与失败状态符合合同 | 通过 | 非法代码不进入请求；行情数据路径未改变 |
| 主操作及权限边界正确 | 通过 | `parseInstrumentId` 统一恢复、切换入口 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | Agent 动作继续经过目录、参数、版本校验 |
| 设置隔离与提醒留存 | 不适用 | 本次未改动 |
| 单元测试、类型检查、构建和烟测 | 通过 | 前端 125/125；`vue-tsc --noEmit` 与 Vite 生产构建通过 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 不适用 | 无视觉改动 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 无壳层改动 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 无导航改动 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 无布局改动 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 无布局改动 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 无行情展示改动 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 无行情展示改动 |
| 模拟账户/仓位/订单有「模拟」标注 | 不适用 | 无交易展示改动 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 无快捷指令展示改动 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 无能力目录改动 |
| <1024px 同路由自适应，无独立移动流 | 不适用 | 无响应式改动 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 不适用 | 无交互结构改动 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 标的与 Agent 上下文一致 | 通过 | 默认、持久化恢复和动作输入共用解析器 |
