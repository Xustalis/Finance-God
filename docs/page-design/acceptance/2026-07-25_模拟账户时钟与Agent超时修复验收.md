# 模拟账户时钟与 Agent 超时修复验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | 同主题设计简报 |
| 受影响路由与页面规范已更新 | 不适用 | 交易台规范无需改变 |
| 真实数据、时间与失败状态符合合同 | 通过 | 历史账户补齐持久模拟时钟；无伪造余额或持仓 |
| 主操作及权限边界正确 | 通过 | 账户所有权与 Agent 权限未改 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 仅模型端点使用 70 秒等待，结果仍来自服务端 |
| 设置隔离与提醒留存 | 不适用 | 未涉及 |
| 单元测试、类型检查、构建和烟测 | 失败 | 实施后填写 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 未改样式 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 通过 | 未改壳层 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 通过 | 未改导航 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 通过 | 未改布局 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 通过 | 未改布局 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 未改行情呈现 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 未改行情 |
| 模拟账户/仓位/订单有「模拟」标注 | 通过 | 恢复原模拟账户与模拟持仓区域 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 快捷指令来源与呈现未改 |
| 「我的」/提醒/设置不进入 Agent 能力 | 通过 | 能力边界未改 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 未改断点 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 未改 DOM 与样式 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 历史账户均有一对一模拟时钟 | 通过 | 数据迁移按缺失行回填 |
| 余额、持仓和成交事实未被迁移改写 | 通过 | 迁移仅写 `simulation_clocks` |
| Agent 超时不暴露 Axios 原始文案 | 通过 | `apiError` 转为中文错误 |
