# “我的”面板与 Agent 布局偏好验收

| 项目 | 状态（通过/失败/不适用） | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_我的面板与Agent布局偏好设计简报.md` 覆盖仿真数据、Agent rail、尺寸、标签顺序、持久化与宽度边界。 |
| 主画布与响应式要求 | 通过 | Playwright 在 1440×1000 验证左右分栏与可调 separator，在 900×1000 验证同一 `/desk` 上下排列；截图为 `output/playwright/desk-completion-1440.png`、`desk-completion-900.png`。 |
| 真实数据、时间与失败状态 | 通过 | 钱包读取 `/simulation/accounts/current`，订单读取 `/simulation/orders`，成交读取 `/simulation/fills`；界面标注仿真、最近读取时间、独立错误与真实空状态。真实浏览器读取到仿真账户 ¥100,000，订单/成交显示服务端空状态。 |
| 主操作及权限边界 | 通过 | 无账户时只能前往持仓建立仿真账户；“我的”不提供资金划转、自动下单或 Agent 动作；设置保存仍调用本人账户接口。 |
| Agent 上下文、快捷指令与工作流 | 通过 | Agent 展开时继续显示当前工作区、标的、context version、三条快捷指令与服务端工作流；收起后只保留可恢复 rail，不暴露“我的”内容。 |
| 设置隔离与提醒留存 | 通过 | 设置表单位于独立 `MyPanel.vue`，提示并保持“不进入 Agent 上下文、快捷指令或工具调用”；提醒入口与记录逻辑未改动。 |
| 键盘、焦点、语义与减弱动效 | 通过 | 分隔线使用可聚焦 `role="separator"`，支持左右方向键与 Home；布局和标签操作均为按钮；订单/成交为语义表格；全局 `prefers-reduced-motion` 规则覆盖新增过渡。 |
| 单元测试、类型检查、构建和烟测 | 通过 | `npm run test`：4 个文件、60 项全部通过；`npm run type-check` 与 `npm run build` 通过；受影响文件定向 ESLint 通过；Playwright 完成钱包、交易记录、Agent rail、1440px 和 900px 烟测。 |

补充：仓库全量 `npm run lint` 仍被现有 `frontend/prototypes/agent-workbench/src/App 2.vue` 的组件命名错误阻断；该文件不属于本次变更，受影响文件的定向 ESLint 无错误。
