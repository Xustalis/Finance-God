# Agent 语义动作控制交易台验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_Agent语义动作控制交易台设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | 仅扩展 `/desk` 既有语义动作合同，无壳层变化 |
| 真实数据、时间与失败状态符合合同 | 通过 | 刷新复用共享真实行情读取；无行情伪造路径 |
| 主操作及权限边界正确 | 通过 | 草稿仅预填；提交、撤单、划转、设置不在目录 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 动作携带 `context_version`，目录来自 bootstrap |
| 设置隔离与提醒留存 | 通过 | 仅可打开指定提醒；未增加设置/已读动作 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端 12 项动作合同测试；前端 88 项测试；`vue-tsc --noEmit`；Vite production build |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 未修改视觉令牌 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 通过 | 顶栏未改 |
| 工作区横向标签；当前朱红底边 | 通过 | 导航未改 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 通过 | 壳层未改 |
| Agent 宽度可调或有布局菜单等价入口 | 通过 | 布局逻辑未改 |
| Agent 收起后可见 rail | 通过 | rail 未改 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 本次不改行情呈现 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 刷新复用既有行情服务 |
| 仿真账户/仓位/订单有「仿真」标注 | 不适用 | 本次不改标注 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 本次不改快捷指令和工作流 |
| 「我的」/提醒/设置不进入 Agent 能力 | 通过 | 目录不含“我的”与设置；提醒仅打开 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 未增加路由或移动流 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 不适用 | 本次未增加独立可视控件 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 四工作区可通过语义动作切换 | 通过 | `navigate_*` 目录与执行器 |
| Agent 不直接操作 DOM、坐标或 CSS 选择器 | 通过 | 服务端显式拒绝 selector 参数 |
| 三类正式回执均可产生 | 通过 | 服务端与前端执行器覆盖 `applied/rejected/stale_context` |
| 未提交交易草稿可预填但不可提交 | 通过 | `fill_trade_draft` 只写入表单 prefill |
