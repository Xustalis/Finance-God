# 交易台服务端上下文同步提速验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_交易台Bootstrap状态一致性设计简报.md` 已明确上下文先 ready、快捷指令渐进加载 |
| 受影响路由与页面规范已更新 | 通过 | `pages/交易台.md` 与 `02_前后端职责与数据合同.md` 已删除 bootstrap 快捷指令合同 |
| 真实数据、时间与失败状态符合合同 | 通过 | 后端测试验证 bootstrap 不调用模型；前端测试验证快捷指令失败不清空有效上下文 |
| 主操作及权限边界正确 | 通过 | bootstrap 仍以服务端能力和 `context_version` 控制 Agent 操作 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 快捷指令唯一读取 `/desk/quick-commands`，迟到响应按请求 ID、工作区、标的和上下文版本丢弃 |
| 设置隔离与提醒留存 | 不适用 | 本次未修改设置、提醒或其 Agent 隔离边界 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端目标 18 项、前端全量 107 项测试通过；`vue-tsc`、ESLint、Vite build 通过；正式交易台组件测试覆盖渐进加载与本地上下文恢复烟测 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 不适用 | 未修改视觉与样式 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 未修改顶栏 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 未修改工作区导航 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 未修改壳层布局 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 未修改 Agent 布局 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 未修改行情展示 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 未修改行情数据链路 |
| 仿真账户/仓位/订单有「仿真」标注 | 不适用 | 未修改仿真业务展示 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 快捷指令成功仍使用服务端三条响应；加载与失败状态独立，未增加静态兜底 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 本次未修改能力目录 |
| <1024px 同路由自适应，无独立移动流 | 不适用 | 未修改响应式布局 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 不适用 | 未修改 DOM、交互控件或动效 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| Bootstrap 成功后不等待快捷指令即可进入 ready | 通过 | `rebuilding.spec.ts` 验证 pending 快捷指令期间上下文已 ready |
| 快捷指令失败不阻断自由输入或清空上下文 | 通过 | 独立失败测试验证 `serverContextVersion` 与 ready 状态保留 |
| 上下文切换时迟到响应不得覆盖新状态 | 通过 | 既有 bootstrap 与快捷指令竞态测试通过 |
| 服务端画像与能力并发装配 | 通过 | 后端使用可控异步屏障验证两个 provider 同时进入 |
| 首次请求复用按用户保存的页面上下文提示 | 通过 | 前端测试验证恢复工作区和标的后只向目标上下文发起一次 bootstrap |
| 本地存储不成为授权事实源 | 通过 | 前端测试验证仅保存 `{section, symbol}`，不保存上下文版本、能力、画像或动作目录 |
