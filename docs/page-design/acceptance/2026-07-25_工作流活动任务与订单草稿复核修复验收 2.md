# 工作流活动任务与订单草稿复核修复验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_工作流活动任务与订单草稿复核修复设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `02_前后端职责与数据合同.md` 已记录唯一活动 Run、409 与草稿版本合同 |
| 真实数据、时间与失败状态符合合同 | 通过 | 真实 PandaData 回归中市场状态监控首次完成；后续 Agent 超时显式落为 `timed_out` |
| 主操作及权限边界正确 | 通过 | 草稿复核绑定当前草稿版本；最终提交未进入工作流 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 服务端选择 `order_review`，客户端仅附带当前草稿版本 |
| 设置隔离与提醒留存 | 不适用 | 本次未改设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端 614 通过/14 跳过；前端类型检查与 88 项测试通过；真实 HTTP/Worker 回归完成 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 不适用 | 无视觉变更 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 无结构变更 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 无结构变更 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 无布局变更 |
| Agent 宽度可调（约 32%–60%）或有布局菜单等价入口 | 不适用 | 无布局变更 |
| Agent 收起后可见 rail（非模态/气泡） | 不适用 | 无布局变更 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 无行情展示变更 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 后续监控沿用已固化快照 `provider_time`，失败保持服务端终态 |
| 仿真账户/仓位/订单有「仿真」标注 | 不适用 | 未改既有标注 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 未改快捷指令；活动冲突恢复服务端 Run |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 未改能力目录 |
| `<1024px` 同路由自适应，无独立移动流 | 不适用 | 无响应式变更 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 不适用 | 无交互控件变更 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 活动 Run 禁止覆盖并可在刷新后恢复 | 通过 | 真实请求首个 201，第二个不同幂等键返回 409 且携带相同 `active_run_id` |
| 草稿、复核、确认、提交均有真实 HTTP 回执 | 通过 | 本次工作流仅复用现有复核服务；确认与提交仍由原独立接口负责 |
