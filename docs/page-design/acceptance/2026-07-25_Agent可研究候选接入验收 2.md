# Agent 可研究候选接入验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_Agent可研究候选接入设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `02_前后端职责与数据合同.md`、`pages/交易台.md` |
| 真实数据、时间与失败状态符合合同 | 通过 | 候选响应含画像版本、方向、PandaData 上游时间；画像/方向/行情不可用均有明确原因；后端候选测试通过 |
| 主操作及权限边界正确 | 通过 | `research_candidates` 为只读工作流且 `allows_trade_eligibility=false`；候选不能直接下单 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 服务端意图路由、正式注册表节点、WorkflowRun 与 Evidence 测试通过 |
| 设置隔离与提醒留存 | 不适用 | 本次未修改设置或提醒能力 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端 592 passed/14 skipped；前端 87 passed；lint 0 error（9 个既有 warning）；Vue 类型检查与 Vite build 通过；Playwright `/desk` 烟测通过 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 未新增样式或颜色；复用现有表格和 Agent Evidence 区 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 本次未修改顶栏 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 本次未修改导航 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 本次未修改壳层 |
| Agent 宽度可调（约 32%–60%）或有布局菜单等价入口 | 不适用 | 本次未修改布局偏好 |
| Agent 收起后可见 rail（非模态/气泡） | 不适用 | 本次未修改收起态 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 本次不展示涨跌 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 候选五维证据显示 provider/upstream time；行情失败保留 missing/unavailable，不生成假候选 |
| 仿真账户/仓位/订单有「仿真」标注 | 不适用 | 本次未修改仿真业务展示 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | Playwright 显示三条指令、`research_candidates`、4/4 节点和确定性 Evidence |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 能力目录未扩大 |
| &lt;1024px 同路由自适应，无独立移动流 | 不适用 | 未改变布局 CSS 或路由 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 复用语义 `button/table/region`；未新增动效 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 候选由服务端画像、持仓与行情共同生成 | 通过 | `candidates_for_profile` + `CandidateScoringService`，API 与 Worker 共用 |
| 候选展示画像版本、方向、五维解释、反方证据与上游时间 | 通过 | Playwright 自选区显示“画像 v3 · equities”、五维解释和 PandaData 时间 |
| Agent 完成后刷新候选区且不直接下单 | 通过 | Pinia 回归测试验证 `research_candidates` 终态调用 `fetchResearchCandidates`；UI 仅提供“申请交易计划” |
