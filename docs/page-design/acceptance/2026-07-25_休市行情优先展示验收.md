# 休市行情优先展示验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_休市行情优先展示设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `pages/交易台.md` 已补充休市最近交易日和逐标的失败合同 |
| 真实数据、时间与失败状态符合合同 | 通过 | `DeskQuote` 保留 OHLC、上游时间、频率、freshness、session alignment；历史批量接口返回逐标的 warnings |
| 主操作及权限边界正确 | 通过 | 刷新与模拟时钟操作未扩权；订单仍使用服务端可信引用价 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 本次未改 Agent |
| 设置隔离与提醒留存 | 不适用 | 本次未改设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端目标测试 13 项、前端完整测试 143 项、`vue-tsc --noEmit`、Vite production build 通过；组件挂载测试覆盖 `/desk` 行情区 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 仅在现有行情元数据行增加文字状态 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 顶栏未改 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 导航未改 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 壳层未改 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | Agent 未改 |
| 涨跌仅用正/风险色，且有非颜色信号 | 通过 | 休市状态由“休市 · 最近交易日”文字表达 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | `MarketChart.vue` 显示市场时段、PandaData 上游时间、频率、freshness；部分失败显示 warnings |
| 模拟账户/仓位/订单有「模拟」标注 | 不适用 | 相关标注未改 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | Agent 未改 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 未改 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 只增加可换行的文本元数据，无新路由或固定宽度 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 未新增交互；错误继续使用 `role="alert"` |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 休市展示最近已发布交易日 | 通过 | `session_alignment=latest_released_session` 映射为“休市 · 最近交易日” |
| 单标的失败不清空批量成功行情 | 通过 | `test_historical_overview_keeps_successful_quotes_when_one_symbol_fails` |
| 成功行情与失败原因同时可见 | 通过 | store 同时写入 `quotes` 与 warning 文本；总览显示“部分行情未显示” |
