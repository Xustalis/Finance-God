# 模拟账户术语与演示持仓验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `docs/page-design/briefs/2026-07-25_模拟账户术语与演示持仓设计简报.md`。 |
| 受影响路由与页面规范已更新 | 通过 | 统一设计规范、数据合同、交易台页面规范和验收模板已改用“模拟”术语。 |
| 真实数据、时间与失败状态符合合同 | 通过 | 模拟买入由 `/simulation/market-orders` 使用 PandaData 服务端可信价成交；页面回执显示标的、成交价、费用和时间。 |
| 主操作及权限边界正确 | 通过 | 本人通过交易表单提交 `000001.SZ` 买入 100 股；服务端返回订单 `order-ef35266029b347d7`，持仓投影随后显示 100 股。 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 仅更新订单气泡用户文案，不改变 Agent 能力或工作流。 |
| 设置隔离与提醒留存 | 不适用 | 未修改设置或提醒。 |
| 单元测试、类型检查、构建和烟测 | 失败 | 类型检查、生产构建、交易成交、持仓展示和图表链接删除烟测通过；相关前端测试 44 通过、9 失败，失败来自当前工作树既有的默认标的、迟到快捷指令、缺失 `fetchBars` mock 与旧 `.instrument-select` 断言；后端 4 项失败来自既有 `simulation_clock` 测试装配和 e2e 路由 404。 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 未修改样式令牌。 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 未修改顶栏。 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 未修改标签。 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 未修改壳层。 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 未修改布局。 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 未修改涨跌语义。 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 交易引用行情显示 `000001.SZ`、¥11.10、上游时间 `2026-07-24T15:00:00+08:00`、频率 `1m` 与 freshness。 |
| 模拟账户/仓位/订单有「模拟」标注 | 通过 | 浏览器显示“模拟账户”“模拟持仓”“模拟交易”“模拟 · 最近 20 条”。 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 未修改快捷指令与工作流。 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 未修改能力边界。 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 仅更新文案和服务端数据，不改变路由与布局。 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 未修改交互结构和样式。 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 用户可见账户、持仓、订单与交易术语统一为“模拟” | 通过 | `frontend/src` 与当前规范扫描无“仿真”残留；浏览器持仓区域无旧术语。 |
| 演示持仓来自服务端交易接口，不是前端静态数据 | 通过 | 交易接口成交后现金从 ¥100,000 更新为 ¥98,889.67，持仓投影新增 `000001.SZ` 100 股。 |
| 持仓表显示标的、数量、成本、市值和盈亏状态 | 通过 | 浏览器持仓表显示 `000001.SZ`、数量/可用 100、成本/市值 ¥1,110、浮盈/已实现 ¥0。 |
| K 线图不显示第三方归因链接 | 通过 | 完整刷新后 `Charting by TradingView` 链接定位数量为 0；初始化配置使用 `layout.attributionLogo = false`。 |
