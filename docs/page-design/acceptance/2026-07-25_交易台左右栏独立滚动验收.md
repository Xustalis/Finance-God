# 交易台左右栏独立滚动验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `docs/page-design/briefs/2026-07-25_交易台左右栏独立滚动设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `docs/page-design/pages/交易台.md` 已明确宽屏独立滚动与窄屏自然滚动 |
| 真实数据、时间与失败状态符合合同 | 不适用 | 本次不改变数据读取或渲染 |
| 主操作及权限边界正确 | 不适用 | 本次不改变业务操作或权限 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 本次不改变 Agent 数据或行为 |
| 设置隔离与提醒留存 | 不适用 | 本次不改变设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | `desk-layout-css.spec.ts` 与 `desk-agent-messages.spec.ts` 共 11 项通过；`npm run type-check`、`npm run build` 通过；Playwright 完成 1440/1023/680px 烟测 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 仅调整尺寸与 overflow，无视觉令牌变化 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 通过 | `TradingDeskView.vue` 未改动 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 通过 | 导航 DOM 与当前态样式未改动 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 通过 | 双栏等宽和双线分隔保留；宽屏主区限制为 `100svh - 70px` |
| Agent 保持可见；无布局设置入口遮挡工作区 | 通过 | Agent 外栏固定在宽屏主区内，未新增布局入口 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 本次不改变行情状态表现 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 本次不改变行情内容 |
| 模拟账户/仓位/订单有「模拟」标注 | 不适用 | 本次不改变模拟业务内容 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 本次不改变快捷指令或工作流 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 本次不改变能力边界 |
| &lt;1024px 同路由自适应，无独立移动流 | 通过 | 窄屏显式恢复上下排列、自动高度和自然页面滚动 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | DOM、焦点和动效规则未改动；滚动容器保持原语义内容 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 1440px 左右栏可分别滚动且顶部栏稳定 | 通过 | Playwright 1440×900：页面 `scrollY=0`；左栏滚至 35px 时 Agent 保持 180px，Agent 滚至 180px 时左栏保持 0；主区高度 830px、顶栏 70px |
| 左栏导航与历史演示时间条不随业务内容滚动 | 通过 | 左栏仅 `.desk-left-content` 为 `overflow-y:auto`；`.desk-workspace-bar` 为固定尺寸 flex 子项，自动化样式合同测试通过 |
| Agent 标题、输入区保持可用，线程/任务历史独立滚动 | 通过 | Playwright 注入长线程后 `scrollHeight=1199`、`clientHeight=576`；滚动前后标题 top 均 70px，输入区 bottom 均 900px |
| 1023px 与 680px 恢复自然页面滚动 | 通过 | Playwright：1023px 为单列、页面高 1477.48px、主区 `overflow:visible`、Agent `height:560px/max-height:none`；680px 同为自动 Agent 高度与自然主区高度 |
