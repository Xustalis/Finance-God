# 访谈请求超时修复验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `docs/page-design/briefs/2026-07-25_访谈请求超时修复设计简报.md` |
| 受影响路由与页面规范已更新 | 不适用 | `pages/` 无访谈页专属规范；统一规范无需变更 |
| 真实数据、时间与失败状态符合合同 | 通过 | 消息仍读取真实后端响应；客户端超时为明确可重试错误 |
| 主操作及权限边界正确 | 通过 | 仅登录用户可提交；认证逻辑未改 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 非交易台 Agent |
| 设置隔离与提醒留存 | 不适用 | 未涉及 |
| 单元测试、类型检查、构建和烟测 | 通过 | `core-behavior.spec.ts` 31 项通过；`vue-tsc --noEmit`、Vite build 通过；浏览器恢复到 60% 完整度且控制台 0 错误 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 未改样式 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 非交易台 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 非交易台 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 非交易台 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 非交易台 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 无行情 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 无行情 |
| 模拟账户/仓位/订单有「模拟」标注 | 不适用 | 无交易数据 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 非交易台 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 未涉及 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 未改布局或路由 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 未改 DOM 或样式 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 回答请求等待预算覆盖后端 AI 超时预算 | 通过 | 前端 70 秒，后端 60 秒 |
| 重试不会重复记录访谈轮次 | 通过 | 前端复用 `request_id`，后端幂等回放 |
| 不显示 Axios 原始超时文案 | 通过 | 超时统一转换为中文可重试错误 |
