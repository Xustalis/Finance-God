# 正式交易台移除历史演示并刷新最新资讯验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `docs/page-design/briefs/2026-07-25_正式交易台移除历史演示并刷新最新资讯设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `docs/page-design/pages/交易台.md` 已声明正式页面无历史演示入口与资讯强制刷新合同 |
| 真实数据、时间与失败状态符合合同 | 通过 | `/market/news` 返回真实来源、发布时间、抓取时间与 freshness；线上 `refresh=1` 验证返回 `cached=false` |
| mock 仅用于获准的非交易只读模块，且模式、原因、时间、来源与恢复行为可验证 | 不适用 | 本次资讯链路不启用 mock；正式复盘不再接收 preview 演示模式 |
| 主操作及权限边界正确 | 通过 | 总览刷新显式传递 `refresh=1`；持仓移除“进入历史演示”操作 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 本次未改动 Agent |
| 设置隔离与提醒留存 | 不适用 | 本次未改动设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | `rebuilding.spec.ts` 44 项通过；`vue-tsc --noEmit` 通过；ESLint 0 error；Vite build 通过；线上部署后补充公网烟测结果 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 未改动令牌与版式 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 通过 | `TradingDeskView.vue` 顶栏未改动 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 通过 | 工作区导航未改动 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 通过 | 壳层未改动 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 通过 | Agent 区未改动 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 本次未改动行情涨跌展示 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 既有行情合同未改动；资讯继续显示发布时间、抓取时间与 freshness |
| mock 未进入价格、估值、Agent/Evidence、风控、订单、成交或审计 | 通过 | 正式交易台无历史演示入口，资讯只接收 `data_mode=real` |
| 模拟账户/仓位/订单有「模拟」标注 | 通过 | 持仓继续显示“模拟账户”“模拟持仓” |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 本次未改动 Agent |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 本次未改动相关能力 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 未新增路由或响应式分支 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 保留原有语义按钮与样式；移除入口未新增交互 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 正式持仓页无历史演示入口 | 通过 | `PortfolioWorkspace.vue` 只显示“实时行情估值” |
| 总览手动刷新强制获取最新真实资讯 | 通过 | `refreshOverviewWorkspace()` 调用 `loadMarketNews(true)`；单测断言 `fetchMarketNews(8, true)` |
| 首次加载允许五分钟内真实缓存 | 通过 | `loadMarketNews()` 默认传递 `refresh=0`；服务端 TTL 为 300 秒 |
| 资讯失败不使用历史演示或 mock | 通过 | `DeskMarketNewsBatch.data_mode` 固定为 `real`，失败写入 `marketNewsError` |
