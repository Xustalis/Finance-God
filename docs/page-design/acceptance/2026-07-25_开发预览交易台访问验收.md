# 开发预览交易台访问验收

| 项目 | 状态（通过/失败/不适用） | 证据 |
|---|---|---|
| 已认证访问 | 通过 | `router.ts` 对 `/desk` 统一使用 `requiresAuth`。 |
| 旧预览参数不旁路 | 通过 | `core-behavior.spec.ts` 验证未认证 `/desk?preview=1` 重定向到登录。 |
| 开发入口 | 通过 | `LoginView.vue` 仅提供 `/login?redirect=/desk`。 |
| 验证 | 通过 | `npm run type-check`、`npm run test -- --run`（53 项）和 `npm run build` 通过。 |
