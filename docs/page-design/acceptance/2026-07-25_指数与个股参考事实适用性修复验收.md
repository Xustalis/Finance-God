# 指数与个股参考事实适用性修复验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_指数与个股参考事实适用性修复设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `pages/交易台.md` 已补充指数与个股事实适用性 |
| 真实数据、时间与失败状态符合合同 | 通过 | 个股生产探测返回 PandaData `get_fina_reports` / `get_margin`；指数由前端状态阻止无效请求 |
| mock 仅用于获准的非交易只读模块，且模式、原因、时间、来源与恢复行为可验证 | 通过 | `CAPABILITY` 不再进入 mock；既有 mock 披露测试继续通过 |
| 主操作及权限边界正确 | 通过 | 指数行情选择与刷新不变；事实仅在 A 股个股上下文读取 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 本次未改 Agent |
| 设置隔离与提醒留存 | 不适用 | 本次未改设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端目标测试 33 项、前端目标测试 36 项、`vue-tsc --noEmit` 与 Vite build 通过 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 复用现有 `.empty-data`，未改令牌 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 本次未改顶栏 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 本次未改标签 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 本次未改壳层 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 本次未改 Agent |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 本次未改行情表现 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 行情链路未变；指数仍使用 PandaData |
| mock 未进入价格、估值、Agent/Evidence、风控、订单、成交或审计 | 通过 | 指数能力不适用不再产生 mock |
| 模拟账户/仓位/订单有「模拟」标注 | 不适用 | 本次未改模拟交易 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 本次未改 Agent |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 本次未改能力目录 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 只替换原事实区域状态内容，无新增布局 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 状态沿用 `role=status`；无新增交互或动效 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 指数选择不请求仅支持个股的事实 API | 通过 | `rebuilding.spec.ts` 验证调用次数不增加 |
| 指数能力不适用不降级为 mock | 通过 | `test_reference_fact_endpoints_do_not_mock_unsupported_instruments` |
| A 股个股恢复真实 PandaData 事实 | 通过 | 生产 `000001.SZ` 两接口返回 provider=PandaData 与真实 facts |
| 指数页面显示适用范围而非刷新失败 | 通过 | `marketFactsNotice` 同时传入两个事实区域的 `role=status` |
