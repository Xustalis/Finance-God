# 交易台 HTTP、候选与 K 线修复验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_交易台HTTP候选与K线修复设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `pages/交易台.md` 已明确分钟/日频合同 |
| 真实数据、时间与失败状态符合合同 | 通过 | 日线请求显式传 `daily`，响应频率不符时抛出可见错误 |
| mock 仅用于获准的非交易只读模块，且模式、原因、时间、来源与恢复行为可验证 | 不适用 | 本次不新增 mock |
| 主操作及权限边界正确 | 通过 | HTTP 写命令使用共享 RFC4122 ID；股票候选不跨方向兜底 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | Agent 待办消息 ID 同样使用共享生成器；其余合同未改 |
| 设置隔离与提醒留存 | 不适用 | 本次未改设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端目标测试 78 项、前端目标测试 80 项、类型检查与 Vite build 通过 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 候选状态复用 `.empty-data` |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 未改顶栏 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 未改标签 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 未改壳层 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 未改壳层 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 未改涨跌 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | K 线频率错配显式失败，不再把分钟聚合冒充日线 |
| mock 未进入价格、估值、Agent/Evidence、风控、订单、成交或审计 | 通过 | 本次无 mock 路径 |
| 模拟账户/仓位/订单有「模拟」标注 | 不适用 | 未改标注 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 未改快捷指令 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 未改能力目录 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 无布局变化 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 候选恢复入口为可聚焦链接；周期仍为原生按钮 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| HTTP 环境写命令不依赖 `crypto.randomUUID` | 通过 | `rebuilding.spec.ts` 使用仅有 `getRandomValues` 的 crypto 完成自选创建 |
| 现金/固收方向显示中文中性状态 | 通过 | `rebuilding.spec.ts` 断言“现金与固收”、`.empty-data`、无 alert |
| 真实候选行情故障保持风险态 | 通过 | 同测试切换 `MARKET_DATA_UNAVAILABLE` 后断言 `.data-error` |
| 首次 K 线读取真实日频 | 通过 | store 测试断言 `fetchBars('000001.SZ', 'daily')` |
| 服务端日线窗口不超过适配器上限 | 通过 | `test_daily_bar_route_uses_adapter_supported_limit` 断言 limit=1000 |
| K 线响应频率错配会被拒绝 | 通过 | `market-bars.spec.ts` 拒绝 daily 请求返回 1分钟 |
