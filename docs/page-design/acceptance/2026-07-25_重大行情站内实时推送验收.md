# 重大行情站内实时推送验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_重大行情站内实时推送设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `pages/交易台.md` 已补 SSE、断线与高优先级规则 |
| 真实数据、时间与失败状态符合合同 | 通过 | 通知 details 引用 MarketAlert 的 symbol/provider_time/detected_at；前端显示断线状态 |
| 主操作及权限边界正确 | 通过 | Toast 隐藏不写已读；历史中的显式操作才调用 read |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 本次未改变 Agent 或工作流合同 |
| 设置隔离与提醒留存 | 通过 | SSE 按认证 owner 查询；通知与事件持久化 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端定向 12 项、前端 SSE 2 项、提醒语义 2 项通过；`vue-tsc` 与 Vite build 通过 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 复用 `.reminder-panel` 与 `.alert-toast` |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 通过 | 未改变交易台顶栏结构 |
| 工作区横向标签；当前朱红底边 | 不适用 | 未改变工作区导航 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 未改变分栏 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 提醒沿用条件浮层 |
| 涨跌仅用正/风险色，且有非颜色信号 | 通过 | 高优先级使用文字前缀 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 提醒显示真实上游时间与检测时间 |
| 模拟账户/仓位/订单有「模拟」标注 | 不适用 | 未改变相关区域 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 未改变 Agent |
| 「我的」/提醒/设置不进入 Agent 能力 | 通过 | 未扩展动作目录 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 复用现有响应式浮层 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 使用 button、aside、role=status |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| SSE 实时进入 Toast 与历史 | 通过 | 后端 owner-scoped stream 测试、前端分块 SSE 解析与提醒组件测试 |
| 游标恢复与跨用户隔离 | 通过 | 后端事件 API 测试 |
| 高优先级提醒不自动消失 | 通过 | TradingDeskView watcher 条件与前端回归测试 |
