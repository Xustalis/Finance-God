# Agent 与工作流真实执行改进验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_Agent与工作流真实执行改进设计简报.md` 已记录服务端分流、任务卡、真实节点、终态证据和失败状态。 |
| 受影响路由与页面规范已更新 | 通过 | 已更新 `pages/交易台.md`：输入先走 `/agent/desk/decision`，简单问题单 Agent 直答，多阶段问题创建 WorkflowRun。 |
| 真实数据、时间与失败状态符合合同 | 通过 | 浏览器显示 PandaData 上游时间、`1m` 与 `unknown`；旧 Worker 抢单时页面明确显示快照失败，没有伪完成；清除旧 Worker 后同一点击完成 12/12。 |
| 主操作及权限边界正确 | 通过 | 浏览器实际点击“分析当前标的行情”产生 decision、`POST /workflows/desk 201`；输入“什么是市盈率？”只产生 decision 与 `/agent/research`，没有创建 WorkflowRun。 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 页面显示服务端 `context_version`、三条快捷指令、执行方式原因、真实角色/服务任务卡；技术记录默认折叠。 |
| 设置隔离与提醒留存 | 通过 | bootstrap 继续固定 `settings_excluded=true`，订单提交/撤单/资金划转为 false；本次未接入“我的”或提醒写操作。 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端 603 tests + 607 subtests 通过、14 跳过，50.18s；Ruff 通过。前端 88 tests、类型检查和生产构建通过，lint 0 error/9 个既有 warning。真实浏览器完成复杂工作流与简单直答。 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 实际浏览器截图为暖米纸面、深墨、细线任务分组；无蓝色选中态，任务卡仅用于同一执行线程。 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 通过 | 浏览器 DOM 快照为 `FINANCE GOD`、副标、提醒、我的。 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 通过 | `/desk` 保留总览/持仓/自选/交易横向标签与既有当前态。 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 通过 | 浏览器保留工作区与右侧 `AI AGENT`，既有 separator/布局实现及测试通过。 |
| Agent 宽度可调（约 32%–60%）或有布局菜单等价入口 | 通过 | 页面存在“布局”、`调整 Agent 面板宽度` separator 与“重置布局”；自动化测试通过。 |
| Agent 收起后可见 rail（非模态/气泡） | 通过 | 既有 rail 结构与布局持久化测试通过，本次未改为模态或气泡。 |
| 涨跌仅用正/风险色，且有非颜色信号 | 通过 | 行情同时显示 `+/-` 符号与数值；本次未改变语义色合同。 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 浏览器真实页面显示 `2026-07-24T15:00:00+08:00`、`1m · unknown`；无价路径保持破折号与错误。 |
| 仿真账户/仓位/订单有「仿真」标注 | 通过 | 既有交易台仿真数据测试保持通过，本次未改变合同。 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 浏览器显示恰好三条指令；任务从 3/12、10/12 自动推进到 12/12。 |
| 「我的」/提醒/设置不进入 Agent 能力 | 通过 | Agent 请求仅包含消息、section、symbol、context version；bootstrap 权限边界测试通过。 |
| &lt;1024px 同路由自适应，无独立移动流 | 通过 | 787×791 in-app browser 仍在 `/desk`，任务卡、证据、输入框均保留。 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | DOM 快照包含 `complementary`、`region`、`heading`、`form/textbox/button`；既有焦点与 reduced-motion 规则未移除。 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 服务端决定直答或工作流，浏览器不自行分类 | 通过 | `/agent/desk/decision` 两次实测分别返回 `answer` 与 `workflow`；前端只消费结果。 |
| 多阶段任务显示真实角色、职责和逐项状态 | 通过 | 浏览器显示 PandaData 行情服务、行情/情绪/新闻研究员、量化监测、保守派审阅、研究经理、组合经理；状态来自 execution audit。 |
| 技术账本不抢占主信息层级 | 通过 | “执行记录与技术信息”默认折叠；工作流 key、revision 不在主任务卡区域。 |
| 完成态同时存在最终产物与 evidence bundle | 通过 | `workflow-d6f7f26895884508bf6ebd68dcf1ba4b` 为 completed、revision 91、12/12、errors=[]；`MarketContext` evidence 有 14 facts、5 inferences、14 sources、8 agent nodes。 |
| 简单问题由一个 Agent 回答且证据先落库 | 通过 | 浏览器输入“什么是市盈率？”显示单 Agent 直答，无任务卡；`agent_run/fg-665711fb0ec54ecf8d793c02f1eb7e22` evidence 为 200、1 fact、1 agent node。 |
| 运行失败显式可见且不返回伪结论 | 通过 | 旧 Worker 竞争导致的快照失败在任务卡和错误区可见；修复后重新点击成功，失败 Run 未生成 final artifact。 |
| 活动 Run 禁止覆盖并可在刷新后恢复 | 通过 | 执行中三条快捷指令、输入框和发送按钮均 disabled；Run ID 写入 `sessionStorage`，恢复测试通过。 |
| 收盘后 stale/unknown 行情保持真实标注 | 通过 | 页面明确显示 `latest_released_session` 对应的上游时间与 `unknown` freshness；工作流结论同时保留未知项，不把其伪装为实时。 |

## 已知非阻断项

- 后端测试仍报告既有 Starlette `httpx` 弃用警告。
- 前端 lint 仍有 9 个既有 `no-explicit-any` warning，均不在本次 Agent/工作流核心改动文件。
- 8000 端口原有旧 Worker 曾抢先领取任务并执行旧代码；该进程已停止。随后重新拉起的 8000 进程加载了当前源码，bootstrap 已实测包含 `agent_answer=true` 与当前完整能力合同。
