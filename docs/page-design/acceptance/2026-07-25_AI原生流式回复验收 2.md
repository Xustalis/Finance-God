# AI 原生流式回复验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_AI原生流式回复设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `pages/交易台.md` 与 `02_前后端职责与数据合同.md` 已记录流式合同 |
| 真实数据、时间与失败状态符合合同 | 通过 | delta 来自 Responses API；服务端显式发送 `error`，前端拒绝缺少 `done` 的响应 |
| 主操作及权限边界正确 | 通过 | 原有 capability/context 门禁不变；UI 动作在流完成后执行 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 工作流仍使用 JSON 与正式 Run；直接回答完成后才请求 `after_answer` 快捷指令 |
| 设置隔离与提醒留存 | 不适用 | 本次未改动设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | Agent 后端路由 13/13；前端 Agent 定向测试 28/28；类型检查、Lint（0 error）与生产构建通过 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 流式文字复用既有纸片消息，仅新增朱红光标 |
| 顶栏、工作区标签与宽屏双栏 | 不适用 | 壳层未改动 |
| Agent 保持可见；无布局设置入口 | 通过 | 增量写入现有 Agent 时间线 |
| Agent 恰好三条快捷指令 + 真实工作流状态 | 通过 | 快捷指令与工作流结构未改动 |
| `<1024px` 同路由自适应 | 通过 | 流式渲染不依赖宽度 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | `aria-live` 保留；减少动态效果时关闭片段动画和光标动画 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 直接回答使用上游原生增量 | 通过 | `OpenAICompatibleChat.stream_complete()` 读取 `response.output_text.delta` |
| 服务端不缓冲完整回答后伪造流 | 通过 | `stream_desk_answer()` 逐次读取上游迭代器并由 `StreamingResponse` 转发 |
| 流中断不产生假完成 | 通过 | `error` 事件和客户端 `AI_STREAM_INCOMPLETE` 显式失败 |
| 新片段有克制淡入效果 | 通过 | `.agent-stream-chunk` 280ms opacity/blur 动画，完成后回归静态正文 |
