# 前端 API 演示故障治理验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_前端API演示故障治理设计简报.md` |
| 受影响路由与页面规范已更新 | 不适用 | 未改变路由、页面结构或服务端合同 |
| 真实数据、时间与失败状态符合合同 | 通过 | `apiBase.ts` 分离 v1/finance；历史模式不请求现实事实或候选 |
| 主操作及权限边界正确 | 通过 | 历史模式不提供现实候选操作；交易与 Agent 权限未改 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | REST 与流式 Agent 统一使用 finance API 根路径 |
| 设置隔离与提醒留存 | 通过 | SSE 改为实际 `/api/events`，游标与持久提醒逻辑未改 |
| 单元测试、类型检查、构建和烟测 | 通过 | 137 项 Vitest、`vue-tsc --noEmit`、Vite build；本地后端 19 个读接口 HTTP 烟测通过 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 复用 `.empty-data` 与 `.data-error` |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 未改动 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 未改动 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 未改动 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 未改动 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 未改动 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 行情请求和错误逻辑未增加 fallback |
| 模拟账户/仓位/订单有「模拟」标注 | 不适用 | 未改动 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 未改动呈现 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 未改动能力合同 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 未增加布局分支 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 预期限制为 `role="status"`，真实失败为 `role="alert"` |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| v1 与 finance API 不再共用错误根路径 | 通过 | `api-base.spec.ts` |
| 提醒 SSE 连接实际 finance 路由 | 通过 | `notification-stream.spec.ts` 断言 `/api/events` |
| 历史演示不请求现实资讯、情绪或候选 | 通过 | `tradingDesk.ts` 在模拟账户存在时提前返回并清空现实状态 |
| 历史限制不误报为接口故障 | 通过 | `rebuilding.spec.ts` 覆盖总览与候选的 status/alert 分流 |
