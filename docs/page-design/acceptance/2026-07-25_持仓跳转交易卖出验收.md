# 持仓跳转交易卖出验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `docs/page-design/briefs/2026-07-25_持仓跳转交易卖出设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `docs/page-design/pages/交易台.md` 已补充持仓卖出跳转合同 |
| 真实数据、时间与失败状态符合合同 | 通过 | 可卖数量来自模拟组合；交易引用价与失败状态继续由既有交易区处理 |
| mock 仅用于获准的非交易只读模块，且模式、原因、时间、来源与恢复行为可验证 | 不适用 | 本次未引入 mock |
| 主操作及权限边界正确 | 通过 | 持仓动作只预填，不调用订单提交接口；交易区仍需用户确认 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 本次未改动 Agent |
| 设置隔离与提醒留存 | 不适用 | 本次未改动设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | `rebuilding.spec.ts` 45 项通过；类型检查通过；ESLint 0 error；生产构建通过 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 使用既有纸面与风险色令牌 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 本次未改动顶栏 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 通过 | 通过现有 `setSection('trading')` 切换工作区 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 本次未改动壳层 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | 本次未改动 Agent |
| 涨跌仅用正/风险色，且有非颜色信号 | 通过 | 卖出使用风险色且有“卖出”文字 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 交易区原有真实行情合同未改动 |
| mock 未进入价格、估值、Agent/Evidence、风控、订单、成交或审计 | 通过 | 未引入 mock 或新事实源 |
| 模拟账户/仓位/订单有「模拟」标注 | 通过 | 持仓与交易标题继续明确标注模拟 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 本次未改动 Agent |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 本次未改动相关能力 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 表格继续使用现有横向滚动 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 使用原生按钮、明确 aria-label 与禁用态 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 持仓行可进入交易区准备卖出 | 通过 | `PortfolioWorkspace.vue` 操作列调用 `preparePositionSell` |
| 标的、方向和数量正确预填 | 通过 | 单测验证标的规范化、`side=sell`、数量等于可卖数量 |
| 跳转不会自动提交订单 | 通过 | 单测验证 `submitSimulationMarketOrder` 未调用 |
| 可卖数量为零时禁止操作 | 通过 | 按钮以 `available_quantity <= 0` 为禁用条件 |
