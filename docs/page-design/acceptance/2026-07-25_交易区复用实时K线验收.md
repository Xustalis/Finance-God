# 交易区复用实时 K 线验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `docs/page-design/briefs/2026-07-25_交易区复用实时K线设计简报.md`；实现复用 `MarketChart` 并同步交易标的 |
| 受影响路由与页面规范已更新 | 通过 | `docs/page-design/pages/交易台.md` 已补充交易 K 线数据、联动与验收规则 |
| 真实数据、时间与失败状态符合合同 | 通过 | 继续使用 store 的 `fetchBars` 与 `quotes`；`MarketChart` 展示 PandaData 时间/freshness；切换标的清空旧 bars，迟到响应被 request id 拒绝 |
| 主操作及权限边界正确 | 通过 | 图表只读；模拟下单原账户、行情、余额/持仓门槛不变 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 本次未改 Agent |
| 设置隔离与提醒留存 | 不适用 | 本次未改设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 失败 | 新增交易 K 线定向测试、`market-chart.spec.ts`、类型检查、lint（0 error）和生产构建通过；`rebuilding.spec.ts` 全文件仍有 8 个与当前工作树既有基线/总览结构相关的失败，非本次功能引入 |

## B. 视觉与壳层

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 直接复用现有 `MarketChart` 与设计令牌 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 壳层未改 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 导航未改 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 壳层未改 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 不适用 | Agent 未改 |
| 涨跌仅用正/风险色，且有非颜色信号 | 通过 | 复用图表的正负号、数值和语义颜色 |
| 行情展示上游时间、频率、freshness；失败无假价 | 通过 | 图表头展示 PandaData 时间/freshness，引用行情继续展示频率；bars 失败走显式错误 |
| 模拟账户/仓位/订单有「仿真」标注 | 通过 | 交易页仍以“模拟交易”和“仿真市价交易”标识业务数据 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | Agent 未改 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 权限边界未改 |
| `<1024px` 同路由自适应，无独立移动流 | 通过 | 新图表位于既有 `/desk` 工作区流内，未新增路由或移动流程 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 周期使用原生按钮和 `aria-pressed`；图表区用带标题的语义 section |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 交易区复用总览实时 K 线 | 通过 | `TradingWorkspace.vue` 直接导入并渲染同一 `MarketChart.vue` |
| 图表、引用价与当前交易股票一致 | 通过 | 确认标的触发 `desk.setSymbol`；图表 quote 使用 `selectedSymbol`，bars 由相同 store symbol 加载 |
| 切换标的无旧图串数据 | 通过 | `setSymbol/loadBars` 清空 bars 并校验 request id、symbol、frequency；`MarketChart` 收到空 bars 时清空全部 series |
| K 线周期切换复用统一频率控制 | 通过 | 交易区将周期事件透传到 `desk.setBarsFrequency`，与总览一致 |
| K 线范围由 PandaData 实际能力决定 | 通过 | 前端 `fetchBars` 不再发送 `limit`；服务端日线从历史起点读取，分钟线合并实际返回数据 |
| 旧 K 线持久缓存并加速加载 | 通过 | `PersistentBarCache` 使用 SQLite 按标的/频率保存完整标准化响应；新鲜缓存跨服务实例直接命中 |
