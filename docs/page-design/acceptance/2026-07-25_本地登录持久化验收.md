# 本地登录持久化验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_本地登录持久化设计简报.md` 描述 30 天本地 JWT 与现有会话恢复 |
| 受影响路由与页面规范已更新 | 不适用 | 路由与页面结构未改变 |
| 真实数据、时间与失败状态符合合同 | 通过 | 身份继续由签名 JWT 与 `/auth/me` 校验，未增加前端伪造状态 |
| 主操作及权限边界正确 | 通过 | 登录、登出、用户与管理员独立会话边界不变 |
| Agent 上下文、快捷指令与工作流符合合同 | 不适用 | 未改 Agent 或工作流 |
| 设置隔离与提醒留存 | 不适用 | 未改设置或提醒 |
| 单元测试、类型检查、构建和烟测 | 通过 | 后端配置测试 8 项、JWT 30 天定向校验、前端会话/路由回归 28 项通过；仅配置与文档变更，类型检查和构建不适用 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 不适用 | 无视觉改动 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 无顶栏改动 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 无工作区改动 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 不适用 | 无布局改动 |
| Agent 宽度可调（约 32%–60%）或有布局菜单等价入口 | 不适用 | 无 Agent 布局改动 |
| Agent 收起后可见 rail（非模态/气泡） | 不适用 | 无 Agent 改动 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 无行情展示改动 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 无行情改动 |
| 仿真账户/仓位/订单有「仿真」标注 | 不适用 | 无仿真数据改动 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 不适用 | 无 Agent 改动 |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 无能力目录改动 |
| <1024px 同路由自适应，无独立移动流 | 不适用 | 无响应式改动 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 不适用 | 无交互结构改动 |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 本地会话跨刷新与服务重启保留 | 通过 | 浏览器仍使用既有 `localStorage`，服务端开发令牌有效期为 43200 分钟 |
| 普通测试账号可重复初始化 | 通过 | `make seed-dev-user` 从本地环境读取凭据，重复执行会轮换密码并恢复 `user/active` |
| 本机交易台入口无需手工输入凭据 | 通过 | `127.0.0.1/localhost` 的 `/login?redirect=/desk` 调用 development-only 登录入口；非 development 返回 404 |
| 生产默认安全边界不被隐式放宽 | 通过 | 代码默认仍为 1440 分钟；生产部署需显式设置环境变量 |
