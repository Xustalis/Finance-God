# 桌面宽度与交易台工作区基础能力验收

## 1. 任务与信息架构

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 页面首屏能识别用户任务、当前对象和主要状态 | 通过 | 1440/1024 浏览器实测保留交易台标题、当前标的与失败状态；900 px 首屏直接说明桌面宽度要求。 |
| 每个区域只有一个明确职责 | 通过 | 布局工具栏仅管理左栏、右栏、底栏与重置；主工作区不参与隐藏。 |
| 主操作唯一且结果可预测 | 通过 | “重置布局”固定恢复三个辅助区域并清除 `finance-god:desk-layout:v1`。 |
| 页面与上一步、下一步对象保持连续 | 不适用 | 本次不改变业务对象或路由流转。 |
| 没有无功能价值的区域或卡片 | 通过 | 仅新增紧凑布局工具栏与不支持宽度提示，无装饰卡片。 |

## 2. 文案

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 标题直接说明区域内容或操作 | 通过 | 使用“工作区”“需要更宽的浏览器窗口”。 |
| 辅助文字说明范围、操作、时效或决策价值 | 通过 | 提示明确最低 1024 px、当前宽度和扩大窗口的恢复方法。 |
| 删除了口号、隐喻、设计说明和重复文案 | 通过 | 新增文案均为操作边界与恢复条件。 |
| 按钮使用“动词 + 对象/结果” | 通过 | “隐藏标的栏”“显示摘要栏”“重置布局”。 |
| 错误、空状态和恢复条件表达明确 | 通过 | 本地存储读取、写入、清除失败均有可见消息；宽度提示给出恢复条件。 |

## 3. 真实数据

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 市场数据来自 PandaData，不含硬编码行情 | 不适用 | 本次未改行情数据。 |
| 显示来源、供应商时间、实际频率和新鲜度 | 不适用 | 本次未改行情展示。 |
| 页面刷新间隔与数据频率分别表达 | 不适用 | 本次未改轮询或频率。 |
| 请求失败不会回退到模拟行情 | 不适用 | 本次未改失败态；浏览器验收用 503 验证现有可见错误。 |
| 真实行情与仿真账户/订单明确分离 | 不适用 | 本次未改业务数据分区。 |
| 浏览器不可访问 PandaData 凭据 | 不适用 | 本次未改数据访问边界。 |

## 4. 工作区与桌面适配

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 1440 px 下信息层级和密度符合规范 | 通过 | Playwright `desk-1440.png`；三栏、底栏、布局工具栏及主工作区同时可见，页面宽度 1440/1440。 |
| 1024 px 下主要任务仍可完成且无页面横向溢出 | 通过 | Playwright 实测 `scrollWidth=clientWidth=bodyScrollWidth=1024`，主区和右栏均存在；见 `desk-1024.png`。 |
| 小于 1024 px 显示桌面端提示 | 通过 | 900 px 实测显示最低 1024 px、当前 900 px 和恢复方法；见 `desktop-notice-900.png`。 |
| 没有移动导航或移动页面 | 通过 | 900 px 时 `.desk-page` 与 AI 侧栏均未挂载，只显示桌面访问提示。 |
| 相关面板可显示、隐藏和调整尺寸 | 失败 | 左栏、右栏、底栏可独立显示/隐藏；本次未实现拖拽调整尺寸。 |
| 工作区布局可以保存、恢复和重置 | 通过 | 浏览器点击隐藏右栏后保存 `{right:false}`，刷新后仍为 `display:none`；重置后存储为 `null`、右栏恢复并显示“布局已重置”。 |

## 5. 视觉

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 使用暖米色报纸终端、衬线字体和紧凑专业工作区视觉 | 通过 | 提示与工具栏复用 `--paper`、`--paper-light`、`--ink` 和全局衬线字体。 |
| 页面没有卡片墙、装饰渐变或常规阴影 | 通过 | 新增界面由细线、纸张面与单列文本组成。 |
| 常规强调、状态、边框和装饰未使用蓝色 | 通过 | 新增样式仅使用墨色、纸色、风险色令牌。 |
| 红绿只用于交易、损益和状态语义 | 通过 | 新增风险色仅用于错误和提示 kicker，未新增绿色装饰。 |
| 数字精度、单位、对齐和时区一致 | 通过 | 宽度统一以整数 px 展示；本次无时间与金额字段。 |

## 6. 状态与交互

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 加载、正常、空、延迟、过期和失败状态已覆盖 | 不适用 | 本次对象是同步浏览器布局偏好，不存在加载、延迟或过期状态；存储失败有显式状态。 |
| 行情刷新不会清除选择或未提交输入 | 不适用 | 本次未改行情刷新。 |
| 隐藏标签页停止轮询，恢复后立即刷新 | 不适用 | 本次未改轮询控制。 |
| 相同请求由统一控制器去重 | 不适用 | 本次无网络请求。 |
| 仿真订单状态未知时不能重复提交 | 不适用 | 本次未改订单提交。 |

## 7. AI

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| AI 分析绑定当前对象和数据版本 | 不适用 | 本次按范围不改 AI。 |
| 显示支持证据、反方证据和未知项 | 不适用 | 本次按范围不改 AI。 |
| AI 不直接修改或提交仿真订单 | 不适用 | 本次按范围不改 AI。 |
| 全部桌面路由常驻同一个 AI 侧栏 | 不适用 | 由并行 AI 任务验收。 |
| 侧栏可收起为可见窄栏并恢复展开 | 不适用 | 由并行 AI 任务验收。 |
| 展开/收起选择可跨路由和刷新恢复 | 不适用 | 由并行 AI 任务验收。 |
| 1024–1279 px 首次访问默认收起 | 不适用 | 由并行 AI 任务验收。 |
| AI 侧栏不使用遮罩且不会遮挡主要操作 | 不适用 | 由并行 AI 任务验收。 |
| 没有悬浮聊天机器人气泡 | 不适用 | 由并行 AI 任务验收。 |

## 8. 可访问性

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 全流程可使用键盘完成 | 通过 | 布局操作均为原生 `button`，无仅指针交互。 |
| 焦点始终可见且浮层关闭后恢复 | 通过 | 使用全局 `:focus-visible`；本次没有浮层。 |
| 表格、标签、对话框和常驻侧栏语义正确 | 通过 | 布局控制为具名 `nav`，辅助区域为具名 `aside`，提示为 `main` + 关联标题。 |
| 状态不只依靠颜色表达 | 通过 | 按钮有文字与 `aria-pressed`，错误与成功状态有完整文本。 |
| 图表提供文字摘要 | 不适用 | 本次未改图表。 |
| 支持 reduced motion | 通过 | 新增组件没有动画或位移动效。 |

## 9. 工程验证

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 行情服务单元测试通过 | 不适用 | 本次未改行情服务。 |
| 前端语法和静态检查通过 | 通过 | `desktop-workspace.spec.ts` 5/5；全量前端 67/67 tests、`vue-tsc --noEmit`、Vite build 通过，ESLint 0 error；`git diff --check` 通过。 |
| 真实 PandaData 请求冒烟测试通过 | 不适用 | 本次按范围不改行情，浏览器验收使用受控 503 验证布局失败态。 |
| 1440、1024 和小于 1024 px 浏览器测试通过 | 通过 | Playwright 分别验证 1440、1024、900 px 并保存截图。 |
| 控制台无未处理错误 | 通过 | 未出现未捕获 JavaScript 异常；控制台资源错误来自验收时刻意注入的 API 503，页面均显示对应可见失败态。 |

## 10. 最终结论

- 验收结论：宽度门槛、1024 无页面横向溢出、面板显隐、跨刷新恢复和重置通过；整体为带一项明确失败的部分通过。
- 未通过项目：面板拖拽调整尺寸尚未实现。
- 已知限制：本次不包含面板排序或拖拽调宽。
- 复现命令或测试入口：
  - `cd frontend && npm test -- src/tests/desktop-workspace.spec.ts`
  - `cd frontend && npx eslint src/App.vue src/components/DesktopWidthNotice.vue src/components/desk/DeskLayout.vue src/composables/useDeskLayoutPreference.ts src/tests/desktop-workspace.spec.ts`
  - Playwright 截图：`output/playwright/desk-1440.png`、`output/playwright/desk-1024.png`、`output/playwright/desktop-notice-900.png`
