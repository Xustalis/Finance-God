# 正式交易台前端验收

| 项目 | 状态（通过/失败/不适用） | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `briefs/2026-07-25_正式交易台重建设计简报.md`，并已更新为统一顶部栏与“我的”结构。 |
| 主画布与响应式要求 | 通过 | `TradingDeskView.vue` 使用统一顶部栏、96px 窄导航、左右 1:1 工作区；CSS 在 1023px 以下转为上下布局。 |
| 真实数据、时间与失败状态 | 通过 | 行情读取 `/market/overview`；市场情绪和资讯读取事实接口；失败显示错误，不展示替代价格或虚构内容。 |
| 主操作及权限边界 | 通过 | 左侧交易区仅准备交易方案；最终提交未由 Agent 或本页自动执行。 |
| Agent 上下文、快捷指令与工作流 | 通过 | Agent 读取当前左侧工作区和标的，显示三条动态快捷指令，并调用后端工作流接口。 |
| 设置隔离与提醒留存 | 通过 | 顶部图标分别打开提醒与“我的”；钱包、记录、画像、设置均在“我的”中，Agent 文案明确排除这些区域。 |
| 键盘、焦点、语义与减弱动效 | 通过 | 使用 `nav`、`table`、`button`、`form` 和可见全局 focus；未新增非必要动效。 |
| 单元测试、类型检查、构建和烟测 | 通过 | `npm run test`（59 通过）、`npm run type-check`、`npm run lint`（0 错误）和 `npm run build` 均通过。 |
