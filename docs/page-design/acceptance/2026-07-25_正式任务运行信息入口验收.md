# 正式任务运行信息入口验收

## A. 流程与合同

| 项目 | 状态 | 证据 |
|---|---|---|
| 设计简报存在且与实现一致 | 通过 | `docs/page-design/briefs/2026-07-25_正式任务运行信息入口设计简报.md` |
| 受影响路由与页面规范已更新 | 通过 | `docs/page-design/pages/交易台.md` 已补充回执入口 |
| 真实数据、时间与失败状态符合合同 | 通过 | 展开气泡只读取现有服务端 Run/Progress；历史任务按 `run_id` 原位加载 |
| 主操作及权限边界正确 | 通过 | 只读查看与刷新，不新增工作流或交易权限 |
| Agent 上下文、快捷指令与工作流符合合同 | 通过 | 正式任务气泡统一承载进度、节点与审计信息，无独立运行区 |
| 设置隔离与提醒留存 | 不适用 | 未改动 |
| 单元测试、类型检查、构建和烟测 | 通过 | 40 条相关测试通过；`vue-tsc --noEmit` 与 Vite 生产构建通过；浏览器实测气泡可展开 |

## B. 视觉与壳层（对照 `01_前端统一设计规范.md`）

| 项目 | 状态 | 证据 |
|---|---|---|
| 纸面/墨色令牌，无品牌蓝与卡片矩阵 | 通过 | 复用 `.refresh-button` 与现有规则线 |
| 顶栏：字标 + 副标；右侧仅提醒与我的 | 不适用 | 未改动 |
| 工作区横向标签；当前朱红底边（`#c91418`） | 不适用 | 未改动 |
| 宽屏双栏 + 双线分隔；Agent 默认可展开 | 通过 | 入口位于既有 Agent 右栏 |
| Agent 保持可见；无布局设置入口遮挡工作区 | 通过 | 无新浮层 |
| 涨跌仅用正/风险色，且有非颜色信号 | 不适用 | 未改动 |
| 行情展示上游时间、频率、freshness；失败无假价 | 不适用 | 未改动 |
| 仿真账户/仓位/订单有「仿真」标注 | 不适用 | 未改动 |
| Agent 恰好三条无卡片快捷指令 + 真实工作流状态 | 通过 | 运行信息来自现有 Run/Progress |
| 「我的」/提醒/设置不进入 Agent 能力 | 不适用 | 未改动 |
| <1024px 同路由自适应，无独立移动流 | 通过 | 789px CSS 视口实测 Agent 高 836px、线程高 593px 且内部滚动 |
| 键盘焦点、语义结构、`prefers-reduced-motion` | 通过 | 使用原生 `button` 和 `details` |

## C. 页面专属

| 项目 | 状态 | 证据 |
|---|---|---|
| 正式任务气泡可展开/折叠运行信息 | 通过 | 浏览器点击摘要后展开运行信息；再次点击可折叠 |
| 历史任务不跳转档案 | 通过 | 历史 Run 在原任务气泡中加载节点、错误与审计元数据 |
| 无独立运行区 | 通过 | DOM 不存在 `aria-label="当前工作流运行状态"` |
| 消息线程不无限拉长页面 | 通过 | 展开前后 Agent 栏均为 836px；线程 `overflow-y:auto`，内容高度由 2214px 增至 2339px 但外栏高度不变 |
