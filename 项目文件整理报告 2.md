# Finance-God 项目文件整理报告

> 生成时间：2026-07-25
> 
> 报告目的：全面分析项目结构、识别问题、提供整理建议

## 1. 项目概览

### 1.1 项目定位
Finance-God 是一个**中文优先的 Agent 主控仿真交易工作台**，面向桌面端用户（零售和专业交易者）。核心特点：
- 左侧工作区：行情、持仓、自选、交易、记录和仿真资金
- 右侧 Agent 面板：常驻 AI 助手，通过工作流和语义动作协调任务
- 真实行情来自 PandaData，所有交易数据标记为仿真

### 1.2 技术栈
- **前端**：Vue 3 + TypeScript + Vite + Pinia + Vue Router
- **后端**：FastAPI + SQLAlchemy 2 + Pydantic + Alembic
- **数据库**：PostgreSQL 16
- **AI 集成**：DeepSeek、StepFun、ARK（OpenAI 兼容接口）
- **行情数据**：PandaData API

### 1.3 项目规模统计

| 类型 | 数量/大小 |
|------|----------|
| 代码文件（.py/.ts/.vue） | 13,812 个 |
| 后端目录大小 | 617 MB |
| 前端目录大小 | 191 MB |
| 文档目录大小 | 548 KB |
| 资源目录大小 | 144 MB |
| 实验产物目录 | 1.5 MB |

## 2. 目录结构分析

```
Finance-God/
├── backend/                    # 后端服务（617MB）
│   ├── finance_god/           # 核心业务逻辑
│   │   ├── domain/           # 领域模型
│   │   ├── application/      # 应用服务层
│   │   ├── execution/        # 订单执行引擎
│   │   ├── market_data/      # PandaData 适配
│   │   ├── orchestration/    # Agent 编排
│   │   ├── infrastructure/   # 持久化层
│   │   └── api/             # HTTP 路由
│   ├── alembic/             # 数据库迁移
│   ├── tests/               # 测试套件
│   ├── vendor/              # 第三方依赖源码
│   └── docs/                # 后端文档
├── frontend/                   # 前端应用（191MB）
│   ├── src/
│   │   ├── views/           # 页面组件
│   │   ├── components/      # 可复用组件
│   │   ├── stores/          # Pinia 状态管理
│   │   ├── services/        # 业务逻辑服务
│   │   ├── api/             # API 客户端
│   │   └── router.ts        # 路由配置
│   └── public/              # 静态资源
├── docs/                      # 项目文档（548KB）
│   ├── prd/                 # 产品需求文档
│   ├── page-design/         # 前端设计规范
│   ├── architecture/        # 架构设计
│   ├── research/            # 调研资料
│   └── experiments/         # 实验记录
├── artifacts/                 # 实验产物（1.5MB）
├── resources/                 # 外部资源（144MB）
├── deploy/                    # 部署配置
└── [配置文件]                 # 根目录配置
```

## 3. 当前工作状态（Git Status）

### 3.1 未提交的更改

**文档类改动**（10 个文件）：
- `AGENTS.md` - Agent 指令文档
- 前端设计规范及模板（6 个）
- 设计简报和验收文档（3 个）

**前端代码改动**（21 个文件）：
- 核心文件：`index.html`, `nginx.conf`, `bootstrap.ts`, `main.ts`
- 交易台组件：`DeskAgentPanel.vue`, `MyPanel.vue`, 各 Workspace 组件
- 服务层：`profile.ts`, `tradingDesk.ts`
- 状态管理：`tradingDesk.ts` store
- 样式：`styles.css`

**删除的资源**（5 个图片文件）：
- 删除了 `frontend/public/masters/` 下的投资大师头像（bogle, buffett, dalio, lynch, seykota）

### 3.2 最近提交历史
```
13582b7 - fix(market-data): use latest released trading session
b8c1b0f - merge: integrate origin/main into unified trading workspace
dc8981e - feat!: rebuild trading desk and workflow platform
06b2a76 - feat:add crawler
61d6a14 - feat:update
```

## 4. 关键文档索引

### 4.1 入口文档

| 文档 | 用途 | 状态 |
|------|------|------|
| `README.md` | 项目总览、安装配置 | ✓ 完整 |
| `AGENTS.md` | 前端开发规范与工作流 | ⚠️ 已修改 |
| `docs/README.md` | 文档中心入口 | ✓ 完整 |
| `docs/项目索引.md` | 代码结构、路由、API 索引 | ✓ 完整 |

### 4.2 规范性文档

| 文档 | 层级 | 约束力 |
|------|------|--------|
| `docs/page-design/01_前端统一设计规范.md` | 现行规范 | 强制 |
| `docs/page-design/02_前后端职责与数据合同.md` | 现行规范 | 强制 |
| `docs/prd/Finance-God_Agent主控交易台_PRD_v2.0.md` | 产品需求 | 强制 |
| `backend/docs/architecture-overview.md` | 架构文档 | 参考 |
| `backend/docs/finance-api-reference.md` | API 规范 | 强制 |

### 4.3 文档优先级规则

冲突时的优先级顺序：
1. 安全、数据完整性和运行时事实
2. 最新产品形态 PRD、交易域 PRD 与前端强制规范
3. 页面专项规格
4. 研究、早期方案和实施计划

## 5. 前端路由与页面状态

### 5.1 已实现路由

| 路由 | 组件 | 状态 | 数据源 |
|------|------|------|--------|
| `/` | 路由守卫 | ✓ 完整 | 本地会话 |
| `/login` | LoginView | ✓ 完整 | `/api/v1` |
| `/app/exe` | OnboardingView | ✓ 完整 | 画像接口 |
| `/app/profile-report` | ProfileReportView | ✓ 完整 | 画像接口 |
| `/overview` | OverviewView | ⚠️ 占位 | 前端状态 |
| `/markets` | MarketsView | ✓ 已接入 | PandaData |
| `/desk` | DeskView | ⚠️ 改动中 | PandaData + 仿真 |
| `/portfolio` | PortfolioView | ⚠️ 占位 | 前端状态 |
| `/orders` | OrdersView | ⚠️ 占位 | 前端状态 |
| `/reviews` | ReviewsView | ⚠️ 占位 | 前端状态 |
| `/data` | DataView | ⚠️ 占位 | 前端状态 |
| `/settings` | SettingsView | ⚠️ 占位 | 前端状态 |
| `/admin/login` | AdminLoginView | ✓ 完整 | 管理员接口 |
| `/admin/ai-settings` | AdminSettingsView | ✓ 完整 | 管理员接口 |

### 5.2 页面实现程度
- ✅ **完整实现**：登录、画像、报告、管理端（4个）
- 🟡 **部分实现**：行情浏览、交易台（2个）
- ⚠️ **占位页面**：总览、组合、订单、复盘、数据、设置（6个）

## 6. 后端架构层次

```
┌─────────────────────────────────────┐
│         API Layer (HTTP)            │
│    /api/v1/* + /api/finance/*       │
└─────────────────────────────────────┘
               ↓
┌─────────────────────────────────────┐
│      Application Services           │
│  账本、投影、冲正、工作区、编排      │
└─────────────────────────────────────┘
               ↓
┌─────────────────────────────────────┐
│      Execution Engine               │
│   订单草稿、风控、撮合、执行         │
└─────────────────────────────────────┘
               ↓
┌─────────────────────────────────────┐
│       Domain Layer                  │
│  业务对象、状态机、不变量            │
└─────────────────────────────────────┘
               ↓
┌─────────────────────────────────────┐
│   Infrastructure (Persistence)      │
│   SQLAlchemy + PostgreSQL           │
└─────────────────────────────────────┘

         External Services
    ┌──────────┬──────────┐
    │ PandaData│   AI     │
    │  (行情)  │ (编排)   │
    └──────────┴──────────┘
```

## 7. 数据库迁移链

当前迁移是线性依赖关系：

```
20260723_0001 (simulation_ledger)
    ↓
20260724_0002 (workflow_runtime)
    ↓
20260724_0003 (simulation_execution)
    ↓
20260724_0004 (workspace_services)
```

每个迁移涵盖的表：
1. **0001**: 仿真账本、账户、现金、持仓与预留
2. **0002**: 工作流运行、事件与审计
3. **0003**: 仿真草稿与订单执行
4. **0004**: 自选、通知、偏好与任务

## 8. 配置文件状态

### 8.1 环境配置

| 文件 | 状态 | 用途 |
|------|------|------|
| `.env` | ✓ 存在（已忽略） | 本地开发配置 |
| `.env.example` | ✓ 已提交 | 配置模板 |
| `deploy/.env.production` | 需手动创建 | 生产环境配置 |
| `backend/.env` | ❌ 已废弃 | 旧配置位置 |

### 8.2 关键环境变量

**必需的生产配置**：
- `DATABASE_URL` / `DATABASE_URL_SYNC`
- `SECRET_KEY`（强密码）
- `PANDA_DATA_USERNAME` / `PANDA_DATA_PASSWORD`
- AI Provider（至少一个）：`DEEPSEEK_API_KEY` 或 `STEPFUN_API_KEY` 或 `ARK_API_KEY`

**前端构建变量**：
- `VITE_WORKBENCH_ORIGIN` 或 `WORKBENCH_ORIGIN`

## 9. 问题识别与建议

### 9.1 ⚠️ 立即需要处理的问题

#### 问题 1：大量未提交的改动
- **影响**：31 个文件修改未提交，影响协作和版本控制
- **建议**：
  1. 审查改动是否完整
  2. 运行测试确保功能正常
  3. 创建有意义的提交信息
  4. 考虑是否需要拆分为多个逻辑提交

#### 问题 2：删除的资源文件未说明
- **影响**：5 个投资大师头像被删除，可能影响现有功能
- **建议**：
  1. 确认这些图片是否仍被代码引用
  2. 如果已废弃，同步更新相关代码
  3. 如果需要替换，准备新资源

#### 问题 3：占位页面较多
- **影响**：6 个主要路由仍是占位实现，用户体验不完整
- **建议**：
  1. 按优先级规划实现顺序（建议：总览 → 组合 → 订单 → 其他）
  2. 每个页面遵循设计简报 → 实现 → 验收流程
  3. 参考 `docs/page-design/templates/` 中的模板

### 9.2 🔍 需要注意的潜在问题

#### 问题 4：大型目录占用
- **backend**: 617MB（vendor/ 第三方源码贡献较大）
- **resources**: 144MB（外部资源）
- **建议**：
  1. 检查 vendor/ 是否可以通过包管理器引入
  2. resources/ 是否有必要全部纳入版本控制
  3. 考虑使用 Git LFS 管理大文件

#### 问题 5：临时目录未清理
- `.playwright-cli/` (311 个文件)
- `.mypy_cache/`
- `.ruff_cache/`
- **建议**：确认 `.gitignore` 已正确配置

#### 问题 6：文档一致性
- 多个架构文档存在版本号或日期（如 `2026-07-24_*`, `2026-07-25_*`）
- **建议**：定期合并或归档历史文档，保持当前文档为单一信息源

### 9.3 📋 改进建议

#### 建议 1：建立清晰的工作流
```bash
# 推荐的开发流程
1. 阅读 docs/page-design/01_前端统一设计规范.md
2. 使用 docs/page-design/templates/前端设计简报模板.md 编写设计
3. 实现功能
4. 使用 docs/page-design/templates/前端设计验收模板.md 自检
5. 提交代码
```

#### 建议 2：完善测试覆盖
当前状态：
- ✓ 后端有单元测试和契约测试
- ✓ 前端有基础测试
- ⚠️ 缺少 E2E 测试
- ⚠️ 缺少 Lint 脚本

建议补充：
```bash
# 前端
cd frontend
npm run lint        # 需要添加
npm run test:e2e    # 需要添加

# 后端
cd backend
.venv/bin/ruff check .     # 代码风格检查
.venv/bin/mypy .           # 类型检查
```

#### 建议 3：优化项目结构
```
建议添加：
├── .vscode/              # VS Code 配置（推荐设置）
├── .editorconfig         # 编辑器配置统一
├── CONTRIBUTING.md       # 贡献指南
├── CHANGELOG.md          # 变更日志
└── docs/
    ├── ONBOARDING.md     # 新成员上手指南
    └── TROUBLESHOOTING.md # 常见问题解决
```

## 10. 核心依赖检查

### 10.1 Python 依赖（后端）
- FastAPI（Web 框架）
- SQLAlchemy 2.x（ORM）
- Alembic（数据库迁移）
- Pydantic（数据验证）
- httpx（HTTP 客户端）

### 10.2 Node.js 依赖（前端）
- Vue 3（UI 框架）
- Vue Router（路由）
- Pinia（状态管理）
- Vite（构建工具）
- Axios（HTTP 客户端）
- lightweight-charts（交易图表）

## 11. API 端点清单

### 11.1 认证相关
- `POST /api/v1/auth/login` - 用户登录
- `POST /api/v1/auth/register` - 用户注册
- `POST /api/v1/admin/login` - 管理员登录

### 11.2 画像相关
- `/api/v1/profile/*` - 投资画像接口

### 11.3 Finance API（行情与交易）
- `GET /api/finance/live` - 存活探针
- `GET /api/finance/ready` - 就绪检查
- `GET /api/finance/health` - 健康状态
- `GET /api/finance/market/quotes` - 批量报价
- `GET /api/finance/market/bars` - K线数据
- `GET /api/finance/market/catalog` - 能力目录
- `/api/finance/workspace/*` - 工作区服务
- `/api/finance/simulation/*` - 仿真交易

## 12. 下一步行动建议

### 立即执行（本周）
1. ✅ **提交当前改动**
   ```bash
   git add -A
   git commit -m "feat: update trading desk components and design docs"
   git push
   ```

2. ✅ **运行完整测试套件**
   ```bash
   # 后端测试
   cd backend && .venv/bin/pytest -v
   
   # 前端测试
   cd frontend && npm test
   ```

3. ✅ **清理临时文件**
   - 确认 `.gitignore` 包含所有临时目录
   - 清理开发缓存

### 短期规划（本月）
1. 实现占位页面（按优先级）
2. 补充 E2E 测试覆盖
3. 完善部署文档和脚本
4. 整理和归档历史文档

### 中期规划（下月）
1. 性能优化（前后端）
2. 安全审计
3. 用户反馈收集与迭代
4. 生产环境就绪检查

## 13. 团队协作建议

### 13.1 分工建议
- **前端开发**：专注 `/desk`、`/portfolio`、`/orders` 等核心页面
- **后端开发**：完善 Agent 编排、优化 PandaData 集成
- **测试工程**：E2E 测试、性能测试
- **DevOps**：部署流程、监控告警

### 13.2 沟通机制
- 每日站会同步进度
- 周会回顾和规划
- 使用 Issue 跟踪任务
- PR Review 强制要求

## 14. 质量检查清单

### 代码质量
- [ ] 所有测试通过
- [ ] 代码风格检查通过
- [ ] 类型检查通过
- [ ] 无明显性能问题
- [ ] 无安全漏洞

### 文档质量
- [ ] README 准确反映当前状态
- [ ] API 文档与实现一致
- [ ] 设计文档完整
- [ ] 注释清晰易懂

### 部署就绪
- [ ] 环境变量配置完整
- [ ] 数据库迁移测试通过
- [ ] 生产配置检查通过
- [ ] 健康检查端点工作正常

---

## 附录：快速命令参考

```bash
# 开发环境启动
make backend          # 启动后端服务
cd frontend && npm run dev  # 启动前端服务

# 数据库操作
cd backend
.venv/bin/alembic upgrade head  # 执行迁移
make seed-dev-admin             # 创建管理员账户

# 测试
cd backend && .venv/bin/pytest -q       # 后端测试
cd frontend && npm test                  # 前端测试

# 代码检查
cd backend && .venv/bin/python -m compileall -q finance_god tests
cd frontend && npm run type-check

# 构建
cd frontend && npm run build

# 生产部署
docker compose --env-file deploy/.env.production \
  -f deploy/docker-compose.prod.yml up -d
```

---

**报告结束**

本报告基于 2026-07-25 的项目状态生成，建议定期更新以反映最新变化。
